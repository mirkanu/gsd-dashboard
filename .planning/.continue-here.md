---
phase: v2.2-project-tasks
task: mid-milestone
total_tasks: 5 phases (17, 18, 19, 20, 15)
status: in_progress
last_updated: 2026-03-29T17:14:41.122Z
---

<current_state>
Milestone v2.2 "Project Tasks" is ~60% complete (3/5 phases done: 17, 18, 20).

Just completed Phase 17 (task data layer) and Phase 18 (task UI) this session.
Then deployed to Railway and hit a 502 — fixed by removing GSD_DATA_URL env var
(was pointing to a dead ephemeral Cloudflare Tunnel). Dashboard now loads, but
the "re-open tmux" button on project cards doesn't work because Railway no longer
proxies to the user's local machine.

User wants tmux/SSH access FROM the public dashboard — this is the main unresolved issue.
</current_state>

<completed_work>

### This session
- Phase 17: Task Data Layer — COMPLETE (verified, committed)
  - 17-01: project_tasks SQLite table + 4 prepared statements + 3 REST endpoints (POST/GET/PATCH)
  - 17-02: 7 test cases for task CRUD — all pass
- Phase 18: Task UI — COMPLETE (verified, committed)
  - 18-01: GsdTask type + api.gsd.tasks client namespace + 9 tests
  - 18-02: TasksTab component (add form, open/archived toggle, optimistic updates) + wired as first tab in GsdDrawer
- Phase 20: Fix Railway Deployment — was already complete from prior session
- Railway 502 fix: Removed GSD_DATA_URL env var (dead Cloudflare Tunnel) — dashboard loads now

### Previously completed
- Quick task 1: Event loop watchdog for Node --watch hangs
- Quick task 2: Alphabetical card sorting + hide paused projects
- Quick task 3: Docker cache-bust for Railway deploy
</completed_work>

<remaining_work>

### Immediate: tmux/SSH access from public dashboard
The user's PRIMARY use case is accessing SSH/tmux sessions from the Railway-hosted dashboard.
Removing GSD_DATA_URL fixed the 502 but broke tmux re-open functionality.

Options discussed:
1. **Persistent Cloudflare Tunnel** (named tunnel, not ephemeral trycloudflare.com) — stable URL that survives reboots
2. **Web terminal via xterm.js + WebSocket** over a persistent tunnel
3. Hybrid: Railway serves UI/tasks, persistent tunnel handles tmux/terminal proxying

Need to research and propose the best approach. This likely needs a new phase or quick task.

### Remaining milestone phases
- Phase 19: Clipboard Export — not yet planned
- Phase 15: New Project Creation (deferred) — has plans but not executed

### Deployment note
Railway URL: https://gsd-dashboard-production.up.railway.app
Auth: basic auth (DASHBOARD_USER=admin, DASHBOARD_PASS in Railway env vars)
GSD_DATA_URL: REMOVED — Railway now uses local SQLite, no upstream proxy
</remaining_work>

<decisions_made>

- Removed GSD_DATA_URL from Railway permanently — ephemeral Cloudflare Tunnel URLs are fundamentally unreliable and cause recurring 502s
- Railway now runs standalone with its own SQLite — no dependency on user's local machine being online
- This trades tmux functionality for dashboard stability — needs a proper solution for remote tmux access
</decisions_made>

<blockers>

- **tmux re-open broken on Railway**: With GSD_DATA_URL removed, the "re-open" button can't reach the user's local tmux sessions. User considers this the main feature they want. Needs architecture decision on how to provide stable remote access to local tmux.
</blockers>

<context>
The user accesses the dashboard exclusively via the public Railway URL — they don't use localhost.
The previous setup had Railway proxying ALL API calls to their local machine via Cloudflare Tunnel,
but this kept breaking because trycloudflare.com URLs are ephemeral (rotate on restart).

The right fix is probably a named Cloudflare Tunnel (cloudflared tunnel create) with a stable
subdomain, OR a different persistent tunneling solution. The dashboard could also be split:
Railway serves the UI + task data, while tmux/terminal features connect directly to the tunnel.

Memory files updated with Railway deployment patterns — check .claude/projects memory.
</context>

<next_action>
Start with: Research and propose a permanent solution for remote tmux access from the Railway dashboard.
Options: named Cloudflare Tunnel, ngrok, Tailscale, or refactoring the proxy to only tunnel tmux/terminal
endpoints while keeping other data local to Railway. Then either /gsd:quick or a new phase to implement.
</next_action>
