---
phase: v2.2-project-tasks
task: mid-milestone
total_tasks: 4 phases (17, 18, 18.1, 19)
status: in_progress
last_updated: 2026-03-29T21:08:11.852Z
---

<current_state>
Milestone v2.2 "Project Tasks" is ~75% complete (3/4 phases done: 17, 18, 18.1).
Phase 20 (fix Railway deployment) also complete from prior work.

This session: completed Phase 18.1 (persistent tunnel) and added PRC project card.
The ngrok tunnel is running and all remote features work from Railway dashboard.
</current_state>

<completed_work>

### This session
- Phase 18.1: Persistent Tunnel for Remote Tmux — COMPLETE
  - 18.1-01: Rewrote tunnel.sh for ngrok (was cloudflared), added systemd service + setup guide
  - 18.1-02: Pivoted from Cloudflare named tunnel to ngrok free static domain, configured Railway, verified end-to-end
  - Tunnel URL: https://heathless-art-unharsh.ngrok-free.dev (permanent, never rotates)
  - All remote features verified by user: terminal overlay, re-open button, session state detection
- Quick task: Added PRC project card to gsd-projects.json with display_name support
  - Added display_name field to project config and API response for custom card titles

### Previously completed (this milestone)
- Phase 17: Task Data Layer (SQLite + CRUD API)
- Phase 18: Task UI (Tasks tab in drawer)
- Phase 20: Fix Railway Deployment (Dockerfile verify-build.sh)
- Quick tasks 1-3: watchdog, alphabetical sort, Docker cache-bust
</completed_work>

<remaining_work>

### Remaining milestone phase
- Phase 19: Clipboard Export — not yet planned
  - "Copy all" button to copy open tasks as formatted markdown
  - Success criteria: button visible when tasks exist, copies `- **Title** — description` lines

### Deployment note
- ngrok tunnel must be running locally for Railway dashboard to access tmux/GSD data
- Currently running as background process; not yet installed as systemd service
- Consider installing systemd service for auto-start on boot: `sh scripts/tunnel-setup.sh` for instructions
</remaining_work>

<decisions_made>

- Pivoted from Cloudflare named tunnel to ngrok — user had no Cloudflare account, ngrok gives free static domain with just email signup
- ngrok authtoken configured at ~/.config/ngrok/ngrok.yml
- NGROK_DOMAIN=heathless-art-unharsh.ngrok-free.dev stored in .env
- GSD_DATA_URL set on Railway to permanent ngrok URL (one-time, never needs updating)
- Added display_name field to gsd-projects.json for custom card titles (PRC vs "prc")
</decisions_made>

<blockers>

None.
</blockers>

<context>
The ngrok tunnel is running as a background process in this session. When the machine reboots
or the process dies, it needs to be restarted manually (sh scripts/tunnel.sh) or installed as
a systemd service. The tunnel self-heals (restarts on failure with 15s backoff).

Railway is deployed and working. All features functional from public URL.
</context>

<next_action>
Start with: /gsd:plan-phase 19 to plan the Clipboard Export phase (last phase of v2.2 milestone).
Or: install ngrok systemd service for auto-start persistence.
</next_action>
