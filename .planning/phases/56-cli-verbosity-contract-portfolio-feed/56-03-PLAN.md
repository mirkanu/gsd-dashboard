---
phase: 56-cli-verbosity-contract-portfolio-feed
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/services/gsddashboard/CLAUDE.md
  - /home/services/debates/CLAUDE.md
  - /home/services/reforma/CLAUDE.md
  - /home/services/ynab/CLAUDE.md
  - /home/services/KidAI/CLAUDE.md
  - /home/services/zoho-todoist-sync/CLAUDE.md
  - /data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md
autonomous: true
requirements:
  - NAR-01
  - NAR-05

must_haves:
  truths:
    - "All 6 project CLAUDE.md files contain the Verbosity Contract section with all 5 rules"
    - "The GSD claude-md.md template contains the Verbosity Contract section so new projects inherit it"
    - "Existing content in each CLAUDE.md is preserved — the section is additive"
    - "The terminal stream is unchanged — only CLAUDE.md instructions are added"
  artifacts:
    - path: "/home/services/gsddashboard/CLAUDE.md"
      provides: "Verbosity Contract section"
      contains: "Skip CONTEXT.md interrogation"
    - path: "/data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md"
      provides: "Verbosity Contract section in workflow enforcement area"
      contains: "Verbosity Contract"
  key_links:
    - from: "/data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md"
      to: "new project CLAUDE.md files"
      via: "GSD project creation uses this template"
      pattern: "Verbosity Contract"
---

<objective>
Apply the 5 CLI verbosity rules to all 6 existing project CLAUDE.md files and the GSD new-project template so future projects inherit them automatically. This is purely additive text editing — no code changes, no server restarts.

Purpose: NAR-01 requires these rules to be in every project CLAUDE.md. The terminal itself is unchanged (NAR-05) — only CLAUDE.md instructions are added so Claude emits fewer words in the terminal.
Output: 6 updated project CLAUDE.md files + 1 updated GSD template.
</objective>

<execution_context>
@/data/home/gsddashboard/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/gsddashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-cli-verbosity-contract-portfolio-feed/56-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Verbosity Contract section to all project CLAUDE.md files</name>
  <files>
    /home/services/gsddashboard/CLAUDE.md,
    /home/services/debates/CLAUDE.md,
    /home/services/reforma/CLAUDE.md,
    /home/services/ynab/CLAUDE.md,
    /home/services/KidAI/CLAUDE.md,
    /home/services/zoho-todoist-sync/CLAUDE.md
  </files>
  <read_first>
    - /home/services/gsddashboard/CLAUDE.md (read current content before modifying)
    - /home/services/debates/CLAUDE.md (read current content before modifying)
    - /home/services/reforma/CLAUDE.md (read current content before modifying)
    - /home/services/ynab/CLAUDE.md (read current content before modifying)
    - /home/services/KidAI/CLAUDE.md (read current content before modifying)
    - /home/services/zoho-todoist-sync/CLAUDE.md (read current content before modifying)
  </read_first>
  <action>
For each of the 6 CLAUDE.md files listed, append the following section at the end of the file (after all existing content). Do NOT replace, restructure, or remove any existing content — this is a pure addition.

The section to append is identical for all 6 files:

```markdown

---

## Verbosity Contract

These rules apply to every terminal session in this project. They reduce what Claude says in the terminal so the tmux pane stays readable.

1. **Skip CONTEXT.md interrogation when CONTEXT.md already exists.** If `.planning/phases/{phase}/{phase}-CONTEXT.md` is present, do not re-interview the user about the phase — proceed directly to planning.
2. **Name the phase in plain English in the first line of the session report.** Instead of "I will now begin Phase 56", write "Starting CLI Verbosity Contract + Portfolio Feed work." One line, present tense, specific.
3. **Don't repeat what the user just said.** If the user said "plan phase 56", do not echo back "You asked me to plan phase 56." Begin the work.
4. **Prefer one-line status updates.** Instead of a paragraph explaining what you are about to do, emit a single line: "Reading roadmap." "Writing plan 01." "Done." Reserve multi-line output for actual results (lists of tasks, file paths, errors).
5. **Active voice, present tense.** Write "Creating feedStore.js" not "feedStore.js will be created" and not "I am in the process of creating feedStore.js".
```

Steps per file:
1. Read the file to see its current ending
2. Append the Verbosity Contract section (preceded by a blank line if the file does not already end with `\n---\n`)
3. Write the updated file
4. Verify the section appears at the bottom with `grep -c "Verbosity Contract" <file>`

Ensure no duplicate section is added if the file already contains "Verbosity Contract" — skip the file in that case. Check with `grep -q "Verbosity Contract" <file>` before appending.
  </action>
  <acceptance_criteria>
    - `grep -c "Verbosity Contract" /home/services/gsddashboard/CLAUDE.md` outputs `1`
    - `grep -c "Verbosity Contract" /home/services/debates/CLAUDE.md` outputs `1`
    - `grep -c "Verbosity Contract" /home/services/reforma/CLAUDE.md` outputs `1`
    - `grep -c "Verbosity Contract" /home/services/ynab/CLAUDE.md` outputs `1`
    - `grep -c "Verbosity Contract" /home/services/KidAI/CLAUDE.md` outputs `1`
    - `grep -c "Verbosity Contract" /home/services/zoho-todoist-sync/CLAUDE.md` outputs `1`
    - `grep -c "Skip CONTEXT.md interrogation" /home/services/gsddashboard/CLAUDE.md` outputs `1`
    - `grep -c "Don't repeat what the user just said" /home/services/debates/CLAUDE.md` outputs `1`
    - `grep -c "Active voice, present tense" /home/services/ynab/CLAUDE.md` outputs `1`
  </acceptance_criteria>
  <verify>
    <automated>grep -l "Verbosity Contract" /home/services/gsddashboard/CLAUDE.md /home/services/debates/CLAUDE.md /home/services/reforma/CLAUDE.md /home/services/ynab/CLAUDE.md /home/services/KidAI/CLAUDE.md /home/services/zoho-todoist-sync/CLAUDE.md | wc -l</automated>
  </verify>
  <done>All 6 files contain exactly one "Verbosity Contract" section. `wc -l` output is `6`. All 5 rules are present in each file.</done>
</task>

<task type="auto">
  <name>Task 2: Add Verbosity Contract to the GSD claude-md.md template</name>
  <files>/data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md</files>
  <read_first>
    - /data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md (full file — understand GSD section marker format and section ordering)
  </read_first>
  <action>
Read `/data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md` first.

The file uses GSD marker-bounded sections (`<!-- GSD:{name}-start -->` / `<!-- GSD:{name}-end -->`). The section ordering is:
1. Project, 2. Stack, 3. Conventions, 4. Architecture, 5. Skills, 6. Workflow Enforcement, 7. Profile

Add a new "Verbosity Contract" section template AFTER the "Workflow Enforcement" section template and BEFORE the "Profile Section" template. The new section follows the same marker pattern as the Workflow Enforcement section:

```markdown
### Verbosity Contract Section
```
<!-- GSD:verbosity-start source:GSD defaults -->
## Verbosity Contract

These rules apply to every terminal session in this project. They reduce what Claude says in the terminal so the tmux pane stays readable.

1. **Skip CONTEXT.md interrogation when CONTEXT.md already exists.** If `.planning/phases/{phase}/{phase}-CONTEXT.md` is present, do not re-interview the user about the phase — proceed directly to planning.
2. **Name the phase in plain English in the first line of the session report.** Instead of "I will now begin Phase 56", write "Starting CLI Verbosity Contract + Portfolio Feed work." One line, present tense, specific.
3. **Don't repeat what the user just said.** If the user said "plan phase 56", do not echo back "You asked me to plan phase 56." Begin the work.
4. **Prefer one-line status updates.** Instead of a paragraph explaining what you are about to do, emit a single line: "Reading roadmap." "Writing plan 01." "Done." Reserve multi-line output for actual results (lists of tasks, file paths, errors).
5. **Active voice, present tense.** Write "Creating feedStore.js" not "feedStore.js will be created" and not "I am in the process of creating feedStore.js".
<!-- GSD:verbosity-end -->
```

Also update the "Section Ordering" list in the template to include step 7: "Verbosity Contract" and shift "Profile" to step 8:

```
1. Project — Identity and purpose
2. Stack — Technology choices
3. Conventions — Code patterns and rules
4. Architecture — System structure
5. Skills — Discovered project skills
6. Workflow Enforcement — Default GSD entry points
7. Verbosity Contract — Terminal output rules
8. Profile — Developer behavioral preferences
```
  </action>
  <acceptance_criteria>
    - `grep -c "GSD:verbosity-start" /data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md` outputs `1`
    - `grep -c "GSD:verbosity-end" /data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md` outputs `1`
    - `grep -c "Verbosity Contract" /data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md` outputs at least `2` (section header + ordering list)
    - `grep -c "Skip CONTEXT.md interrogation" /data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md` outputs `1`
    - `grep -n "GSD:verbosity-start" /data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md` shows the line appears AFTER the `GSD:workflow-end` line
  </acceptance_criteria>
  <verify>
    <automated>grep -n "GSD:verbosity-start\|GSD:verbosity-end\|GSD:workflow-end\|GSD:profile-start" /data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md</automated>
  </verify>
  <done>Template has GSD:verbosity-start and GSD:verbosity-end markers. verbosity section appears after workflow-end and before profile-start in the file. Section ordering list updated to 8 items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md content → Claude sessions | Instructions in CLAUDE.md govern Claude's terminal output behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-08 | Tampering | CLAUDE.md content modification | accept | Changes are additive (append only) and reviewed text — no code execution paths affected; existing behavior unchanged |
| T-56-09 | Denial of Service | Verbosity rules suppressing important output | accept | Rules target ceremonial narration only; errors, file paths, and task results are explicitly excluded ("Reserve multi-line output for actual results") |
</threat_model>

<verification>
```bash
# Confirm all 6 project files updated
for f in /home/services/gsddashboard/CLAUDE.md /home/services/debates/CLAUDE.md /home/services/reforma/CLAUDE.md /home/services/ynab/CLAUDE.md /home/services/KidAI/CLAUDE.md /home/services/zoho-todoist-sync/CLAUDE.md; do
  count=$(grep -c "Verbosity Contract" "$f" 2>/dev/null || echo 0)
  echo "$f: $count"
done

# Confirm template updated
grep -n "GSD:verbosity-start\|GSD:verbosity-end" /data/home/gsddashboard/.claude/get-shit-done/templates/claude-md.md
```
</verification>

<success_criteria>
- All 6 project CLAUDE.md files contain exactly 1 "Verbosity Contract" section with all 5 rules
- The GSD claude-md.md template has a verbosity section with GSD marker format
- No existing content was removed or restructured in any file
- `grep -c "Active voice, present tense"` returns `1` for each of the 6 project CLAUDE.md files
</success_criteria>

<output>
After completion, create `.planning/phases/56-cli-verbosity-contract-portfolio-feed/56-03-SUMMARY.md`
</output>
