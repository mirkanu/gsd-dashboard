---
phase: 20-fix-railway-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - client/scripts/verify-build.sh
autonomous: false
requirements: []

must_haves:
  truths:
    - "The live Railway URL serves the alphabetically-sorted project grid"
    - "Paused projects are hidden (collapsed section) in the live dashboard"
    - "Future Railway builds fail fast if a client build produces no output"
  artifacts:
    - path: "client/scripts/verify-build.sh"
      provides: "Post-build assertion that dist/index.html exists and is non-empty"
    - path: "Dockerfile"
      provides: "Calls verify-build.sh after npm run build to catch silent build failures"
  key_links:
    - from: "Dockerfile Stage 2 RUN npm run build"
      to: "client/scripts/verify-build.sh"
      via: "RUN bash scripts/verify-build.sh"
      pattern: "verify-build"
    - from: "Railway deploy"
      to: "live URL https://gsd-dashboard-production.up.railway.app"
      via: "railway up --detach"
      pattern: "gsd-dashboard-production"
---

<objective>
Deploy commit 33be559 (the dequal patch fix) to Railway so the live dashboard reflects the alphabetical sort and paused-card hiding from commit 4fc84c5. Then add a Dockerfile post-build assertion so future deploys cannot silently serve empty/stale client builds.

Purpose: The root cause (missing client/scripts/patch-dequal.cjs) is now fixed in git. The Railway deployment just needs to be triggered with the correct commit. The safeguard prevents this class of failure from recurring.

Output: Live Railway dashboard showing alphabetical cards + paused section collapsed. Dockerfile with a verify-build.sh assertion after the Vite build step.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

Railway deploy command:
  RAILWAY_API_TOKEN=$(grep RAILWAY_API_TOKEN /data/home/gsddashboard/.env | cut -d= -f2) railway up --detach

Live URL: https://gsd-dashboard-production.up.railway.app

Key commits:
  4fc84c5 — alphabetical sort + paused section feature (client/src/pages/GSD.tsx)
  33be559 — committed client/scripts/patch-dequal.cjs + Dockerfile build guard (this is the fix)

Current Dockerfile Stage 2 structure (as of 33be559):
  COPY client/package.json client/package-lock.json ./
  RUN npm ci                        # postinstall runs patch-dequal.cjs
  COPY client/ ./
  RUN test -f scripts/patch-dequal.cjs || exit 1
  RUN npm run build                 # Vite build
  (no check that dist/ was actually produced)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add post-build dist verification and deploy</name>
  <files>client/scripts/verify-build.sh, Dockerfile</files>
  <action>
    Step 1 — Create client/scripts/verify-build.sh:

    ```bash
    #!/bin/bash
    # Asserts that the Vite build produced a non-empty dist/index.html.
    # Run after `npm run build` in the Dockerfile to catch silent build failures.
    set -e
    DIST="dist/index.html"
    if [ ! -f "$DIST" ]; then
      echo "ERROR: $DIST not found — Vite build produced no output. Aborting."
      exit 1
    fi
    SIZE=$(wc -c < "$DIST")
    if [ "$SIZE" -lt 500 ]; then
      echo "ERROR: $DIST is suspiciously small ($SIZE bytes). Aborting."
      exit 1
    fi
    echo "Build OK: $DIST exists ($SIZE bytes)"
    ```

    Make it executable: chmod +x client/scripts/verify-build.sh

    Step 2 — Update Dockerfile Stage 2. After the existing `RUN npm run build` line, add:
    ```
    RUN bash scripts/verify-build.sh
    ```

    The Stage 2 block should now end with:
    ```
    RUN test -f scripts/patch-dequal.cjs || (echo "ERROR: client/scripts/patch-dequal.cjs missing" && exit 1)
    RUN npm run build
    RUN bash scripts/verify-build.sh
    ```

    Step 3 — Commit both files:
    ```
    git add client/scripts/verify-build.sh Dockerfile
    git commit -m "fix: add post-build dist assertion to Dockerfile to catch silent build failures"
    git push origin master
    ```

    Step 4 — Deploy to Railway:
    ```
    RAILWAY_API_TOKEN=$(grep RAILWAY_API_TOKEN /data/home/gsddashboard/.env | cut -d= -f2) railway up --detach
    ```

    Step 5 — Wait ~3 minutes, then check HTTP status:
    ```
    curl -s -o /dev/null -w "%{http_code}" https://gsd-dashboard-production.up.railway.app
    ```
    Expected: 200 or 401 (auth gate). Either confirms the server is running.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://gsd-dashboard-production.up.railway.app</automated>
  </verify>
  <done>verify-build.sh exists and is committed. Dockerfile calls it after npm run build. railway up --detach succeeded. Live URL returns 200 or 401.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live dashboard reflects alphabetical sort and paused hiding</name>
  <files>none</files>
  <action>Human verification — no code changes. Confirm the deployed dashboard shows the expected UI.</action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://gsd-dashboard-production.up.railway.app</automated>
  </verify>
  <done>Human confirms: project cards are sorted alphabetically and paused projects are hidden/collapsed on the live Railway dashboard.</done>
  <what-built>Deployed the dequal-patch fix and post-build assertion to Railway. The live dashboard should now reflect the alphabetical sort and paused-cards-hidden feature from commit 4fc84c5.</what-built>
  <how-to-verify>
    1. Visit https://gsd-dashboard-production.up.railway.app and log in
    2. Navigate to the GSD tab (project grid)
    3. Confirm project cards appear in alphabetical order
    4. Confirm paused projects are NOT shown inline — they should be in a collapsed "Paused" section or hidden entirely
    5. If Railway is still building: check https://railway.app/project/2415b1c4-c414-4d3c-bd13-6ca1d5ed3750 for build logs
    6. If the build log shows an error, paste the last 20 lines here for diagnosis
  </how-to-verify>
  <resume-signal>Type "approved" if the alphabetical sort and paused hiding are visible. Describe what you see if something is wrong.</resume-signal>
</task>

</tasks>

<verification>
- client/scripts/verify-build.sh exists and is executable
- Dockerfile Stage 2 calls `RUN bash scripts/verify-build.sh` after `RUN npm run build`
- `git log --oneline -3` shows the fix commit pushed to master
- `curl https://gsd-dashboard-production.up.railway.app` returns 200 or 401
- Human confirmed: alphabetical sort visible + paused section hidden in live dashboard
</verification>

<success_criteria>
1. Live Railway dashboard at https://gsd-dashboard-production.up.railway.app shows project cards sorted A-Z
2. Paused projects are not mixed into the main grid (collapsed or hidden)
3. Dockerfile has a post-build assertion (verify-build.sh) that will catch future empty-dist failures
4. All changes are committed and pushed to master
</success_criteria>

<output>
After completion, create `.planning/phases/20-fix-railway-deployment/20-01-SUMMARY.md` following the summary template.
</output>
