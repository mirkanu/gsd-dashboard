---
phase: 21-card-ux-simplification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/GSD.tsx
autonomous: true
requirements:
  - CARD-04

must_haves:
  truths:
    - "Project cards show only: project name, state label, status badges, live URL, and Open Terminal button"
    - "Progress bar, phase summary, requirements coverage, last activity, next action, blockers, and velocity/streak stats are absent from card face"
    - "Expandable roadmap section is removed from the card"
    - "Archive / Unarchive button remains on the card"
  artifacts:
    - path: "client/src/pages/GSD.tsx"
      provides: "Slim ProjectCard component with removed sections"
      contains: "ProjectCard"
  key_links:
    - from: "ProjectCard"
      to: "card header"
      via: "project name + state label + StatusBadge + liveUrl"
      pattern: "stateConf.label|liveUrl|StatusBadge"
---

<objective>
Strip the project card face down to only the information that matters at a glance: project name, session state label, status badge(s), live URL, and Open Terminal. Remove progress, stats, next action, last activity, blockers, and the expandable roadmap panel. This reduces visual noise so users can scan the grid quickly.

Purpose: Dense cards slow scanning. The drawer already provides full detail — the card only needs to answer "what state is this project in and how do I jump into it?"
Output: Modified ProjectCard in GSD.tsx with the body sections stripped to essential-only.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current ProjectCard structure in client/src/pages/GSD.tsx (lines 492-728) -->

Sections currently in the card (in order):
  1. Header (lines 516-558): project name, version badge, state label, Blocked badge, StatusBadge, current_phase text, milestone_name text, liveUrl link — KEEP all of this
  2. Progress section (lines 560-573): ProgressBar + phase summary + requirements coverage — REMOVE
  3. Last activity (lines 575-582): last_activity text — REMOVE
  4. Next action (lines 584-591): next_action text — REMOVE
  5. Blockers (lines 593-603): blocker list with AlertCircle icons — REMOVE
  6. Stats row (lines 605-622): velocity, streak, estimatedCompletion — REMOVE
  7. Terminal button (lines 624-664): Open terminal / Re-open button — KEEP
  8. Archive/Unarchive button (lines 666-685): — KEEP
  9. Expandable roadmap (lines 687-726): collapsible phases panel — REMOVE

Props still needed after slimming:
  project.display_name, project.name, project.version, project.sessionState,
  project.liveUrl, project.state?.status (for StatusBadge), project.state?.blockers (for Blocked badge only — keep the badge in header),
  project.tmuxActive, project.tmuxSession (for terminal button),
  project.sessionState (for archive/unarchive)

Props that can be removed from usage (still in type, just not rendered):
  project.state?.progress, project.state?.last_activity, project.state?.next_action,
  project.roadmap, project.requirements, project.velocity, project.streak,
  project.estimatedCompletion, project.state?.current_phase (optional: may keep for minimal context),
  project.state?.milestone_name

Helper components that become unused and can be removed from file:
  - ProgressBar (only used in progress section + roadmap)
  - RoadmapPanel
  - PhaseIcon
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slim down ProjectCard — remove body sections, keep header + terminal + archive</name>
  <files>client/src/pages/GSD.tsx</files>
  <action>
In the ProjectCard function (starting around line 492):

**Keep in the card:**
- The header section: project name, version badge, state label (stateConf.label), Blocked badge (state?.blockers check), StatusBadge (state?.status)
- The liveUrl link inside the header
- The current_phase text line (keeps one line of context — acceptable minimal info)
- The milestone_name text line (keeps one line of context — acceptable minimal info)
- The terminal button section (Open terminal / Re-open)
- The Archive / Unarchive button

**Remove these sections entirely** (cut the JSX blocks and their surrounding conditional wrappers):
- Progress section: the `<div className="px-4 py-3 border-b...">` containing ProgressBar, phaseSummary, requirements count
- Last activity section: the `{state?.last_activity && (...)}` block
- Next action section: the `{state?.next_action && (...)}` block
- Blockers section: the `{state?.blockers && state.blockers.length > 0 && (...)}` block at line 593
- Stats row: the `{(project.velocity > 0 || project.streak > 0 || ...) && (...)}` block
- Expandable roadmap: the `{roadmap && roadmap.phases.length > 0 && (...)}` block including the button and expanded panel

**Remove now-unused destructured variables** from inside ProjectCard:
- `const { state, roadmap, requirements } = project;` — after removing roadmap/requirements usage, update destructuring to just `const { state } = project;`
- Remove `const progress = state?.progress;`
- Remove `const phaseSummary = ...`
- Remove `const [expanded, setExpanded] = useState(false);`

**Remove now-unused helper components** that are no longer referenced anywhere:
- `ProgressBar` function (lines ~74-84)
- `RoadmapPanel` function (lines ~88-108)
- `PhaseIcon` function (lines ~64-70)

**Remove now-unused imports:**
- `CheckCircle2, Circle, Clock, AlertCircle, ChevronDown, ChevronRight, Layers, ClipboardList` from lucide-react — check each is no longer used anywhere after the removal (PhaseIcon used CheckCircle2/Circle/Clock/AlertCircle; expandable roadmap used ChevronDown/ChevronRight/Layers/ClipboardList). Keep `MapPin` and `ExternalLink` as they remain in the header.

The final card structure should be:
  ```
  <div className="card flex flex-col gap-0 overflow-hidden cursor-pointer {stateConf.border}">
    {/* Header: name + version + state label + Blocked badge + StatusBadge */}
    {/* liveUrl link */}
    {/* current_phase text (optional one-liner) */}
    {/* milestone_name text (optional one-liner) */}

    {/* Terminal button (tmuxActive / Re-open) */}
    {/* Archive / Unarchive */}
  </div>
  ```

Do NOT remove the `expanded` state and roadmap from the GSD() component's TAB_TITLES or GsdDrawer wiring — those remain intact. Only ProjectCard is modified.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>ProjectCard renders only name/state/badges/liveUrl/terminal/archive sections; ProgressBar, RoadmapPanel, PhaseIcon functions removed; unused lucide imports removed; build compiles with no TypeScript errors or unused variable warnings</done>
</task>

<task type="auto">
  <name>Task 2: Update GsdProject.test.ts to reflect slim card — verify type shape is unchanged</name>
  <files>client/src/components/__tests__/GsdProject.test.ts</files>
  <action>
The GsdProject type itself does NOT change — only the rendering removes some fields from the card face. The existing tests in GsdProject.test.ts test the type shape, which is preserved.

Review the test file and confirm all existing tests still pass by running the test suite. No test changes are needed unless the build emits TypeScript errors about unused fields.

If `npm run test:client` passes without changes, this task is complete. If there are failures, diagnose and fix — likely an import that was removed from GSD.tsx that the test file indirectly depended on.

Run: `npm run test:client 2>&1 | tail -30`
  </action>
  <verify>
    <automated>npm run test:client 2>&1 | tail -30</automated>
  </verify>
  <done>All client tests pass; no regressions from card slimming</done>
</task>

</tasks>

<verification>
- `npm run build` compiles cleanly
- `npm run test:client` passes all tests
- ProjectCard no longer renders: progress bar, phase summary, requirements %, last activity, next action, blockers list, velocity/streak stats, expandable roadmap
- ProjectCard still renders: project name, version badge, state label, Blocked badge (from header), StatusBadge, liveUrl, terminal button, archive/unarchive button
</verification>

<success_criteria>
- CARD-04: Cards show only project name, state indicator, status badges, live URL, and Open Terminal. Stats, progress, next action, and blockers are absent from the card face.
- Build passes, all tests pass, no unused import warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/21-card-ux-simplification/21-02-SUMMARY.md`
</output>
