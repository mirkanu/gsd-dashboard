---
phase: 80-claude-mem-cross-project-memory
plan: 02
type: execute
wave: 2
depends_on: [80-01]
files_modified:
  - /home/claude/.claude/settings.json
autonomous: true
requirements: [MEM-02]

must_haves:
  truths:
    - "Every new Claude Code SessionStart triggers the claude-mem context hook automatically"
    - "The SessionStart hook starts the worker if not already running (idempotent)"
    - "The SessionStart hook queries for relevant past observations and injects them into context"
    - "Existing GSD hooks (check-update, hook-handler, session-state) continue to work alongside claude-mem"
    - "PostToolUse hook captures tool observations and sends them to the worker"
  artifacts:
    - path: "/home/claude/.claude/settings.json"
      provides: "SessionStart hooks array with claude-mem entries"
      contains: "context-hook.js"
  key_links:
    - from: "SessionStart hook"
      to: "worker-service.cjs"
      via: "bun start (idempotent)"
      pattern: "worker-service.cjs start"
    - from: "SessionStart hook"
      to: "context-hook.js"
      via: "queries worker for relevant observations"
      pattern: "context-hook.js"
    - from: "PostToolUse hook"
      to: "save-hook.js"
      via: "sends tool results to worker"
      pattern: "save-hook.js"
---

<objective>
Wire the claude-mem SessionStart and PostToolUse hooks into the global Claude Code settings so every session across all projects automatically gets context injection from the shared memory store and feeds observations back into it.

Purpose: This is the "make it work everywhere" plan — without these hooks, the worker runs but no session uses it. After this plan, every Claude Code session on the VPS transparently gets cross-project memory.
</objective>

<execution_context>
@/data/home/gsddashboard/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/gsddashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80/80-RESEARCH.md
@.planning/phases/80/80-01-SUMMARY.md

<interfaces>
<!-- Current settings.json SessionStart hooks (from Plan 01 context) -->
From /home/claude/.claude/settings.json:
```json
"SessionStart": [
  { "hooks": [{ "type": "command", "command": "\"/usr/bin/node\" \"/data/home/.claude/hooks/gsd-check-update.js\"" }] },
  { "hooks": [{ "type": "command", "command": "node \"/data/home/gsddashboard/scripts/hook-handler.js\" SessionStart" }] },
  { "hooks": [{ "type": "command", "command": "bash \"/data/home/.claude/hooks/gsd-session-state.sh\"" }] }
]
```

<!-- Current PostToolUse hooks -->
```json
"PostToolUse": [
  { "matcher": "Bash|Edit|Write|MultiEdit|Agent|Task", "hooks": [{ "type": "command", "command": "\"/usr/bin/node\" \"/data/home/.claude/hooks/gsd-context-monitor.js\"", "timeout": 10 }] },
  { "matcher": "*", "hooks": [{ "type": "command", "command": "node \"/data/home/gsddashboard/scripts/hook-handler.js\" PostToolUse" }] },
  { "matcher": "Write|Edit", "hooks": [{ "type": "command", "command": "bash \"/data/home/.claude/hooks/gsd-phase-boundary.sh\"", "timeout": 5 }] },
  { "matcher": "Read", "hooks": [{ "type": "command", "command": "\"/usr/bin/node\" \"/home/claude/.claude/hooks/gsd-read-injection-scanner.js\"", "timeout": 5 }] }
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add claude-mem SessionStart hooks to global settings.json</name>
  <files>/home/claude/.claude/settings.json</files>
  <read_first>
    - /home/claude/.claude/settings.json
    - /home/services/gsddashboard/.planning/phases/80/80-RESEARCH.md
    - /home/services/gsddashboard/.planning/phases/80/80-01-SUMMARY.md
  </read_first>
  <action>
1. Read the current `/home/claude/.claude/settings.json` to confirm its exact current state.

2. Find the `SessionStart` array in the `hooks` object. Add TWO new hook entries at the BEGINNING of the array (before existing GSD hooks, so worker starts before GSD hooks run):

```json
{
  "matcher": "startup|clear|compact",
  "hooks": [
    {
      "type": "command",
      "command": "\"/home/claude/.bun/bin/bun\" \"/home/claude/.claude/plugins/marketplaces/thedotmack/claude-mem/plugin/scripts/worker-service.cjs\" start",
      "timeout": 60
    },
    {
      "type": "command",
      "command": "\"/home/claude/.bun/bin/bun\" \"/home/claude/.claude/plugins/marketplaces/thedotmack/claude-mem/plugin/scripts/context-hook.js\"",
      "timeout": 60
    }
  ]
}
```

The `matcher: "startup|clear|compact"` ensures the hooks fire on every session start type (fresh session, /clear, /compact).

3. Use the absolute bun path `/home/claude/.bun/bin/bun` (confirmed in Plan 01) to ensure PM2/systemd compatibility.

4. The final SessionStart array should have 5 entries: [claude-mem worker start, claude-mem context injection, gsd-check-update, hook-handler, gsd-session-state].

5. Validate the JSON is well-formed: `python3 -c "import json; json.load(open('/home/claude/.claude/settings.json'))"` should print nothing (success).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('/home/claude/.claude/settings.json')); ss=[h for h in d.get('hooks',{}).get('SessionStart',[]) if 'context-hook' in str(h)]; print('claude-mem hooks found:', len(ss))"</automated>
  </verify>
  <acceptance_criteria>
    - `/home/claude/.claude/settings.json` is valid JSON (python3 json.load succeeds)
    - SessionStart array contains an entry with `"context-hook.js"` in the command string
    - SessionStart array contains an entry with `"worker-service.cjs"` in the command string
    - The claude-mem entries appear BEFORE the gsd-check-update entry in the array
    - The matcher for claude-mem hooks is `"startup|clear|compact"`
    - All existing GSD hooks (gsd-check-update, hook-handler, gsd-session-state) remain present
  </acceptance_criteria>
  <done>SessionStart hooks array in settings.json now includes claude-mem worker start + context injection as the first two entries, preserving all existing GSD hooks.</done>
</task>

<task type="auto">
  <name>Task 2: Add claude-mem PostToolUse hook for observation capture</name>
  <files>/home/claude/.claude/settings.json</files>
  <read_first>
    - /home/claude/.claude/settings.json
    - /home/services/gsddashboard/.planning/phases/80/80-RESEARCH.md
  </read_first>
  <action>
1. Read the current `/home/claude/.claude/settings.json` (after Task 1 modifications).

2. Find the `PostToolUse` array in the `hooks` object. Add ONE new hook entry at the END of the array (after existing hooks, so observation capture is non-blocking):

```json
{
  "matcher": "*",
  "hooks": [
    {
      "type": "command",
      "command": "\"/home/claude/.bun/bin/bun\" \"/home/claude/.claude/plugins/marketplaces/thedotmack/claude-mem/plugin/scripts/save-hook.js\"",
      "timeout": 120
    }
  ]
}
```

3. Use the absolute bun path `/home/claude/.bun/bin/bun` for PM2/systemd compatibility.

4. The `matcher: "*"` ensures every tool use triggers observation capture. The 120s timeout accommodates slow tool outputs (e.g., large Bash results).

5. Validate the JSON is still well-formed: `python3 -c "import json; json.load(open('/home/claude/.claude/settings.json'))"` should print nothing.

6. Verify the hook count: the PostToolUse array should now have 5 entries (gsd-context-monitor, hook-handler, gsd-phase-boundary, gsd-read-injection-scanner, save-hook).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('/home/claude/.claude/settings.json')); ptu=[h for h in d.get('hooks',{}).get('PostToolUse',[]) if 'save-hook' in str(h)]; print('save-hook entries:', len(ptu))"</automated>
  </verify>
  <acceptance_criteria>
    - `/home/claude/.claude/settings.json` is valid JSON
    - PostToolUse array contains an entry with `"save-hook.js"` in the command string
    - The save-hook entry has `"matcher": "*"`
    - The save-hook entry has `"timeout": 120`
    - All existing PostToolUse hooks (gsd-context-monitor, hook-handler, gsd-phase-boundary, gsd-read-injection-scanner) remain present
    - Total PostToolUse entries: 5
  </acceptance_criteria>
  <done>PostToolUse hooks now include claude-mem save-hook as the final entry, ensuring every tool call across all projects feeds observations to the shared worker without blocking existing hooks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SessionStart hook | Plugin code runs at session initialization with user's full permissions |
| PostToolUse hook | Tool outputs (potentially including sensitive data) are sent to worker for compression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-06 | Information Disclosure | save-hook.js (tool outputs) | accept | Worker respects `<private>` tags in Claude Code output; single-user VPS with no access boundary needed |
| T-80-07 | Tampering | SessionStart hook order | mitigate | claude-mem hooks placed first so worker is running before any other hooks attempt MCP operations |
| T-80-08 | Denial of Service | SessionStart timeout | mitigate | Each hook has 60s timeout; claude-mem context injection adds ~2-3s on cold start, negligible on warm worker |
| T-80-09 | Elevation of Privilege | Plugin scripts | accept | Plugin runs as the claude user with no elevated permissions; cannot exceed user's own access |
</threat_model>

<verification>
- `python3 -c "import json; d=json.load(open('/home/claude/.claude/settings.json')); print('SessionStart entries:', len(d['hooks']['SessionStart'])); print('PostToolUse entries:', len(d['hooks']['PostToolUse']))"`
- SessionStart array has 5 entries, PostToolUse array has 5 entries
- SessionStart first entry contains "context-hook.js"
- PostToolUse last entry contains "save-hook.js"
- All existing hooks remain present
</verification>

<success_criteria>
- SessionStart hooks include claude-mem worker start + context injection (first two entries)
- PostToolUse hooks include claude-mem save-hook (last entry)
- All existing GSD hooks remain functional
- JSON is valid and parseable
- Hooks use absolute bun path for PM2/systemd compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/80/80-02-SUMMARY.md`
</output>
