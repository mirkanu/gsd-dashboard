---
phase: 80-claude-mem-cross-project-memory
plan: 03
type: execute
wave: 3
depends_on: [80-02]
files_modified:
  - /home/services/.claude-mem/resource-footprint.md
autonomous: false
requirements: [MEM-03, MEM-04]

must_haves:
  truths:
    - "A solution from Project A surfaces in a Project B session via search() without manual CLAUDE.md updates"
    - "Observations accumulate in the SQLite store during active Claude Code sessions"
    - "MCP tools (search, timeline, get_observations) are available in every session"
    - "Resource footprint is documented: RSS under 512MB, disk growth rate measured"
  artifacts:
    - path: "/home/services/.claude-mem/resource-footprint.md"
      provides: "Documented RAM, CPU, and disk usage after 24h of operation"
      contains: "RSS"
  key_links:
    - from: "Session B search()"
      to: "SQLite observations store"
      via: "semantic recall from Project A"
      pattern: "search.*result"
---

<objective>
Verify cross-project recall works end-to-end and document the actual resource footprint after 24h of operation. This is the UAT plan — it proves the system works and captures baseline metrics for MEM-03 and MEM-04.

Purpose: Without this verification, we cannot claim Phase 80 is complete. This plan provides the evidence that cross-project memory actually functions and fits within VPS resource budget.
</objective>

<execution_context>
@/data/home/gsddashboard/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/gsddashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80/80-RESEARCH.md
@.planning/phases/80/80-01-SUMMARY.md
@.planning/phases/80/80-02-SUMMARY.md

<interfaces>
<!-- Worker API endpoints (from RESEARCH.md) -->
Worker health: GET http://127.0.0.1:37700/api/health
Observations: GET http://127.0.0.1:37700/v1/observations?limit=N
SQLite DB: /home/services/.claude-mem/claude-mem.db
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify observation capture and cross-project recall via worker API</name>
  <files>/home/services/.claude-mem/resource-footprint.md</files>
  <read_first>
    - /home/services/gsddashboard/.planning/phases/80/80-RESEARCH.md
    - /home/services/gsddashboard/.planning/phases/80/80-01-SUMMARY.md
    - /home/services/gsddashboard/.planning/phases/80/80-02-SUMMARY.md
  </read_first>
  <action>
1. Confirm the worker is still running: `curl -s http://127.0.0.1:37700/api/health | python3 -m json.tool` should show `"status": "ok"`.

2. Check that observations have been accumulating since Plan 01 (the worker has been running during any Claude Code sessions):
```bash
curl -s "http://127.0.0.1:37700/v1/observations?limit=3" | python3 -m json.tool
```

3. If the observations list is empty (no sessions have run since worker started), run a brief Claude Code session on any project to generate observations. Then re-check.

4. Verify the SQLite DB exists and has tables:
```bash
sqlite3 /home/services/.claude-mem/claude-mem.db ".tables"
sqlite3 /home/services/.claude-mem/claude-mem.db "SELECT COUNT(*) FROM observations;"
```

5. Test cross-project recall via the search endpoint:
```bash
curl -s "http://127.0.0.1:37700/v1/search?query=dashboard&limit=3" | python3 -m json.tool
```
This should return semantic matches from any project that has discussed "dashboard". If you get results with project names different from the current one, cross-project recall is confirmed.

6. If the search endpoint is not available at `/v1/search` (it may be MCP-only), test it from inside a Claude Code session by running `/mcp` and checking that `mcp-search` is listed with tools `search`, `timeline`, `get_observations`.

7. Record the result: at least one observation exists in the DB, and either the search endpoint returns results or the MCP tools are registered in a session.
  </action>
  <verify>
    <automated>curl -s "http://127.0.0.1:37700/v1/observations?limit=1" | python3 -c "import sys,json; d=json.load(sys.stdin); print('observations:', len(d.get('observations',[])))"</automated>
  </verify>
  <acceptance_criteria>
    - `curl -s http://127.0.0.1:37700/api/health` returns `"status": "ok"`
    - `sqlite3 /home/services/.claude-mem/claude-mem.db "SELECT COUNT(*) FROM observations;"` returns a number > 0
    - Either the /v1/search endpoint returns results OR the MCP tools are confirmed registered in a Claude Code session
    - The observations contain entries from the gsddashboard project (or whatever project was active during Plan 01-02 execution)
  </acceptance_criteria>
  <done>Observations are confirmed accumulating in the shared store. Cross-project recall is verified via either the search API or MCP tool registration.</done>
</task>

<task type="auto">
  <name>Task 2: Measure and document resource footprint</name>
  <files>/home/services/.claude-mem/resource-footprint.md</files>
  <read_first>
    - /home/services/gsddashboard/.planning/phases/80/80-RESEARCH.md
    - /home/services/gsddashboard/.planning/phases/80/80-01-SUMMARY.md
  </read_first>
  <action>
1. Measure worker RSS memory:
```bash
ps -o rss=,vsz=,comm= -p $(pgrep -f worker-service)
```
Record RSS in MB (RSS value / 1024).

2. Measure Chroma Python process memory (if running):
```bash
ps -o rss=,comm= -p $(pgrep -f chroma) 2>/dev/null || echo "chroma process not found"
```

3. Measure total claude-mem data directory:
```bash
du -sh /home/services/.claude-mem/
du -sh /home/services/.claude-mem/chroma/ 2>/dev/null || echo "no chroma dir"
ls -lh /home/services/.claude-mem/claude-mem.db*
```

4. Check PM2 memory report:
```bash
pm2 env $(pm2 pid claude-mem-worker) | grep -i "memory\|rss" || pm2 list
```

5. Check disk growth rate (if the worker has been running since Plan 01, compare current size to the size recorded in Plan 01). If only 1-2 hours have passed, note "insufficient uptime for growth rate" and estimate monthly based on current observation count.

6. Write `/home/services/.claude-mem/resource-footprint.md`:
```markdown
# Claude-Mem Resource Footprint

**Measured:** YYYY-MM-DD HH:MM
**Worker uptime:** X hours

## RAM

| Component | RSS (MB) | Notes |
|-----------|----------|-------|
| worker-service.cjs (Bun) | X | Main worker process |
| Python/Chroma | X | Embedding model + vector DB |
| **Total** | X | Budget: 512MB |

## Disk

| Path | Size | Notes |
|------|------|-------|
| claude-mem.db | X | SQLite observations store |
| claude-mem.db-wal | X | WAL file |
| chroma/ | X | Vector embeddings |
| **Total** | X | Budget: ~120MB/month growth |

## CPU

| Metric | Value | Notes |
|--------|-------|-------|
| Worker idle CPU | ~0% | Only active during observation compression |
| Compression calls | X/min | During active sessions |

## Verdict

- RAM: within 512MB budget YES/NO
- Disk: within 120MB/month estimate YES/NO
- Fits VPS budget (~730MB free RAM, 6.8GB free disk): YES/NO
```

7. If RSS exceeds 400MB, investigate: check `ps aux | grep chroma` and consider disabling vector search if the embedding model is too large.
  </action>
  <verify>
    <automated>test -f /home/services/.claude-mem/resource-footprint.md && grep -q "RSS" /home/services/.claude-mem/resource-footprint.md && grep -q "Budget" /home/services/.claude-mem/resource-footprint.md</automated>
  </verify>
  <acceptance_criteria>
    - `/home/services/.claude-mem/resource-footprint.md` exists
    - File contains "worker-service" (Bun process identified)
    - File contains "RSS" memory measurement
    - File contains "Budget" comparison
    - File contains "Disk" section with claude-mem.db size
    - File contains "Verdict" section with YES/NO for budget fit
    - Total RSS is under 512MB
  </acceptance_criteria>
  <done>Resource footprint documented in /home/services/.claude-mem/resource-footprint.md with actual measurements. RAM under 512MB budget confirmed. Disk growth rate estimated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT — verify MCP tools appear in a fresh session</name>
  <what-built>
    The full claude-mem integration: worker running in PM2, SessionStart hooks injecting context, PostToolUse hooks capturing observations, shared SQLite+Chroma store at /home/services/.claude-mem/.
  </what-built>
  <how-to-verify>
    1. Open a new terminal session and start Claude Code on any project (e.g., `cd /home/services/gsddashboard && claude`).
    2. Within 5 seconds of session start, run `/mcp` in the Claude Code prompt.
    3. Verify that `mcp-search` appears in the list of MCP servers with tools: `search`, `timeline`, `get_observations`.
    4. Ask Claude: "What do you know about [some topic you worked on in a previous session]?" — Claude should reference past observations if any exist.
    5. Do a few actions (read a file, run a command), then exit the session.
    6. Start a DIFFERENT project's session (e.g., `cd /home/services/other-project && claude`).
    7. Ask Claude about the same topic from step 4 — if cross-project recall works, Claude should surface relevant context from the first session.
  </how-to-verify>
  <resume-signal>Type "approved" if MCP tools appear and cross-project recall works (or observations are empty because no prior sessions generated data), or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cross-project observation access | All projects share one observation store — a session in Project A can theoretically search Project B's observations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-10 | Information Disclosure | Shared observation store | accept | Single-user VPS — all projects belong to the same user; no access boundary needed |
| T-80-11 | Denial of Service | Search query in fresh session | mitigate | Search is async with timeout; worker process is not blocked by slow queries |
</threat_model>

<verification>
- Worker health endpoint returns ok
- SQLite DB has observations (COUNT > 0)
- MCP tools (search, timeline, get_observations) appear in /mcp listing
- Resource footprint documented with actual numbers
- Total RAM usage under 512MB
- Disk usage under 200MB for initial install
</verification>

<success_criteria>
- Observations are confirmed in the shared store
- MCP tools are accessible in a Claude Code session
- Cross-project recall works OR no error occurs (empty store is acceptable if no sessions ran)
- Resource footprint documented: RAM < 512MB, disk < 200MB initial
- Human confirms MCP tools appear in /mcp listing
</success_criteria>

<output>
After completion, create `.planning/phases/80/80-03-SUMMARY.md`
</output>
