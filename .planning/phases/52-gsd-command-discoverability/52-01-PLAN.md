---
phase: 52
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - GSD Command Discoverability — CLAUDE.md guidance for proactive /gsd-* command suggestions

must_haves:
  truths:
    - "Claude suggests /gsd-next when user says 'what should I do next?' or 'keep going'"
    - "Claude suggests /gsd-resume-work when user says 'pick up where I left off' or 'continue my work'"
    - "Claude suggests /gsd-progress when user says 'how is the project going?' or 'give me a status update'"
    - "Claude suggests /gsd-plan-phase when user says 'plan the next phase' or 'create a plan'"
    - "Claude suggests /gsd-execute-phase when user says 'start building' or 'execute the plan'"
    - "Claude suggests /gsd-quick when user says 'do a quick task' or 'small fix'"
    - "Claude suggests /gsd-pause-work when user says 'stop for now' or 'save my progress'"
    - "Claude suggests /gsd-verify-work when user says 'check if it works' or 'run the tests'"
  artifacts:
    - path: "CLAUDE.md"
      provides: "GSD Command Suggestions section with NL → command mapping table"
      contains: "## GSD Command Suggestions"
  key_links:
    - from: "CLAUDE.md GSD Command Suggestions section"
      to: "Claude's proactive suggestion behavior"
      via: "Natural language pattern matching in Claude's reasoning"
      pattern: "GSD Command Suggestions"
---

<objective>
Add a "GSD Command Suggestions" section to CLAUDE.md that teaches Claude to proactively
suggest the most relevant /gsd-* command when the user describes what they want in natural
language. The section provides a lookup table mapping user intent phrases to the correct
command, so Claude surfaces underused commands rather than asking the user to look them up.

Purpose: Reduce friction for users who know what they want but don't know which /gsd-* command
to use. Makes the GSD toolkit discoverable through natural conversation.

Output: CLAUDE.md with a new "GSD Command Suggestions" section containing an intent-to-command
mapping table with 10+ patterns.
</objective>

<execution_context>
@/data/home/gsddashboard/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/gsddashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GSD Command Suggestions section to CLAUDE.md</name>
  <files>CLAUDE.md</files>

  <read_first>
    - CLAUDE.md — read entire file to understand current structure before appending
    - /data/home/gsddashboard/.claude/get-shit-done/workflows/help.md — read to extract authoritative command descriptions for the mapping table
  </read_first>

  <action>
Append a new section to the end of `/home/services/gsddashboard/CLAUDE.md` with the heading
`## GSD Command Suggestions` (two newlines before it to separate from the prior section).

The section must contain:
1. One introductory sentence: "When the user describes what they want in plain English, suggest the most relevant `/gsd-*` command from this table rather than asking them to look it up."
2. A markdown table with columns: `| User says (examples) | Suggest |`
3. The following rows in the table (exact text):

| User says (examples) | Suggest |
|---|---|
| "what's next?", "keep going", "what should I do?", "advance the project", "continue" | `/gsd-next` |
| "pick up where I left off", "resume", "I'm back", "restore context" | `/gsd-resume-work` |
| "how is it going?", "project status", "what have we done?", "show progress" | `/gsd-progress` |
| "stop for now", "save my place", "I need a break", "pause" | `/gsd-pause-work` |
| "plan the next phase", "create a plan", "I want to plan phase N" | `/gsd-plan-phase N` |
| "start building", "execute the plan", "run the phase", "build it" | `/gsd-execute-phase N` |
| "small fix", "quick task", "do this one thing", "minor change" | `/gsd-quick` |
| "check if it works", "run the tests", "verify what we built", "did it work?" | `/gsd-verify-work` |
| "I want to start a new project", "new project", "initialize" | `/gsd-new-project` |
| "I have an idea for a phase", "I want to describe my vision", "discuss the plan" | `/gsd-discuss-phase N` |
| "debug this", "something is broken", "fix this error" | `/gsd-debug` |
| "what commands are available?", "help", "what can GSD do?" | `/gsd-help` |

4. After the table, add one note paragraph: "If the user's intent is ambiguous between two commands, name both and let the user pick. Never silently pick one without saying so."

Write the modified CLAUDE.md using the full file content with the new section appended.
  </action>

  <verify>
    <automated>grep -n "GSD Command Suggestions" /home/services/gsddashboard/CLAUDE.md && grep -n "gsd-next" /home/services/gsddashboard/CLAUDE.md && grep -n "gsd-resume-work" /home/services/gsddashboard/CLAUDE.md && grep -c "| User says" /home/services/gsddashboard/CLAUDE.md</automated>
  </verify>

  <acceptance_criteria>
    - CLAUDE.md contains the exact heading `## GSD Command Suggestions`
    - CLAUDE.md contains a table row with `/gsd-next` as the Suggest value
    - CLAUDE.md contains a table row with `/gsd-resume-work` as the Suggest value
    - CLAUDE.md contains a table row with `/gsd-pause-work` as the Suggest value
    - CLAUDE.md contains a table row with `/gsd-quick` as the Suggest value
    - CLAUDE.md contains a table row with `/gsd-verify-work` as the Suggest value
    - CLAUDE.md contains the line `| User says (examples) | Suggest |`
    - `grep -c "^|" /home/services/gsddashboard/CLAUDE.md` returns at least 13 (header + separator + 12 data rows)
    - The note paragraph ending with "without saying so" is present
  </acceptance_criteria>

  <done>
    CLAUDE.md contains a complete GSD Command Suggestions section with a table mapping
    12+ natural language intent patterns to specific /gsd-* commands, plus the ambiguity
    resolution note. Claude will use this section to proactively suggest commands when
    users describe what they want.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md content → Claude behavior | CLAUDE.md is read by Claude; malicious content could alter behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | CLAUDE.md | accept | File is in the project repo under git; any modification is tracked and reversible. Low risk: single-user local tool, not a public API. |
| T-52-02 | Information Disclosure | CLAUDE.md command table | accept | Table contains only public command names; no credentials or secrets. Low risk. |
</threat_model>

<verification>
1. `grep -n "## GSD Command Suggestions" /home/services/gsddashboard/CLAUDE.md` — returns a line number
2. `grep -n "gsd-next" /home/services/gsddashboard/CLAUDE.md` — appears in table
3. `grep -c "^| " /home/services/gsddashboard/CLAUDE.md` — at least 14 lines (header + separator + 12 rows)
4. `git diff CLAUDE.md` shows only the appended section
</verification>

<success_criteria>
- CLAUDE.md has a new GSD Command Suggestions section at the end
- Section contains a table with 12+ NL-to-command rows
- Table includes /gsd-next, /gsd-resume-work, /gsd-progress, /gsd-pause-work, /gsd-plan-phase, /gsd-execute-phase, /gsd-quick, /gsd-verify-work, /gsd-new-project, /gsd-discuss-phase, /gsd-debug, /gsd-help
- Ambiguity resolution note is present
- No other sections of CLAUDE.md are modified
</success_criteria>

<output>
After completion, create `.planning/phases/52-gsd-command-discoverability/52-01-SUMMARY.md`
</output>
