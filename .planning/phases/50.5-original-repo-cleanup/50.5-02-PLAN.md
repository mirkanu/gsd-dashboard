---
phase: 50.5-original-repo-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed.js
  - scripts/import-history.js
  - statusline/
  - server/index.js
  - server/routes/settings.js
autonomous: true
requirements:
  - CLN-04
must_haves:
  truths:
    - "scripts/seed.js, scripts/import-history.js, and the statusline/ directory no longer exist in the repo"
    - "server/index.js does not require or invoke any function from scripts/import-history.js at startup or in the periodic maintenance sweep"
    - "server/routes/settings.js no longer imports or invokes import-history"
    - "Server boots successfully with the dropped imports — schema bootstrap + route mounting complete without error"
    - "npm run test:server passes"
    - "/api/gsd/projects still returns 200 with unchanged response shape"
  artifacts:
    - path: "server/index.js"
      provides: "Server boot + periodic sweep without import-history dependency"
      contains: "setInterval"
  key_links:
    - from: "server/index.js"
      to: "none"
      via: "removed require of ../scripts/import-history"
      pattern: "import-history"
---

<objective>
Delete the scripts/seed.js, scripts/import-history.js, and statusline/ directory — all inherited from the upstream fork and unused by the current dashboard. Remove every server-side reference to import-history so the server boots without them. No behaviour change for shipped dashboard features.

Purpose: CLN-04. Clears dead scripts and the standalone statusline directory the project never used.
Output: Smaller repo surface; server startup path simplified.
</objective>

<execution_context>
@/data/home/gsddashboard/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/gsddashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/50.5-original-repo-cleanup/50.5-CONTEXT.md
@CLAUDE.md
@.claude/rules/backend-node.md
@.claude/rules/docs-markdown.md
@server/index.js
@server/routes/settings.js
@scripts/import-history.js
@scripts/seed.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete seed/import-history scripts and the statusline/ directory (CLN-04 filesystem portion)</name>
  <files>
    scripts/seed.js (delete),
    scripts/import-history.js (delete),
    statusline/ (delete directory)
  </files>
  <read_first>
    scripts/seed.js (confirm no unexpected external importers before deletion),
    scripts/import-history.js (note exported symbols: importCompactions, findCompactionsInFile, importAllSessions, backfillCompactions — all referenced from server/index.js + server/routes/settings.js, which Task 2 removes before this check runs; for safety run the grep FIRST in the action),
    statusline/ (confirm contents are just the upstream standalone statusline — README.md, statusline-command.sh, statusline.py — not referenced anywhere in the project),
    package.json (confirm no npm scripts point to seed.js, import-history.js, or statusline/)
  </read_first>
  <action>
    1. BEFORE deleting, prove nothing outside server/index.js + server/routes/settings.js (handled in Task 2) references these scripts. Run:
       - `grep -rn "scripts/seed\|require.*seed\.js\|from.*seed" . --include='*.js' --include='*.ts' --include='*.json' --include='*.md' --exclude-dir=node_modules --exclude-dir=.planning` → expected: only internal references inside scripts/seed.js itself (plus package.json npm script if any).
       - `grep -rn "import-history\|importAllSessions\|importCompactions\|findCompactionsInFile\|backfillCompactions" . --include='*.js' --include='*.ts' --include='*.json' --exclude-dir=node_modules --exclude-dir=.planning` → expected: only server/index.js, server/routes/settings.js, and scripts/import-history.js itself.
       - `grep -rn "statusline/\|\"./statusline\"\|statusline-command\.sh\|statusline\.py" . --include='*.js' --include='*.ts' --include='*.json' --include='*.md' --exclude-dir=node_modules --exclude-dir=.planning --exclude-dir=statusline` → expected: no matches outside the statusline/ directory itself. Note: .claude/hooks/gsd-statusline.js is a DIFFERENT file (Claude Code hook, not the upstream directory) — keep it.
       - If package.json has any script pointing at seed.js or import-history.js, remove that script entry in this task (edit package.json). Do NOT remove Claude Code hook statusline references.
    2. After grep proves safety, delete:
       - `rm scripts/seed.js`
       - `rm scripts/import-history.js`
       - `rm -rf statusline/`
    3. If grep finds an unexpected caller outside the known set, STOP and surface the caller path in the plan summary rather than deleting.
  </action>
  <acceptance_criteria>
    - `test -f scripts/seed.js` exits 1
    - `test -f scripts/import-history.js` exits 1
    - `test -d statusline` exits 1
    - `test -f .claude/hooks/gsd-statusline.js` exits 0 (the unrelated Claude hook file is preserved)
    - `grep -rn "scripts/seed\|scripts/import-history\|\"./statusline\"" . --include='*.js' --include='*.ts' --include='*.json' --exclude-dir=node_modules --exclude-dir=.planning` returns no matches AFTER Task 2 completes (this task alone deletes the files; remaining server references are cleaned in Task 2 — but the paired PLAN execution leaves no dangling refs by end of plan)
  </acceptance_criteria>
  <verify>
    <automated>cd /data/home/gsddashboard &amp;&amp; bash -c '! test -f scripts/seed.js &amp;&amp; ! test -f scripts/import-history.js &amp;&amp; ! test -d statusline &amp;&amp; echo ok' | grep -q ok</automated>
  </verify>
  <done>All three upstream-inherited artifacts deleted; grep proof of zero external callers captured in the plan summary.</done>
</task>

<task type="auto">
  <name>Task 2: Remove import-history references from server/index.js + server/routes/settings.js (CLN-04 server portion)</name>
  <files>
    server/index.js (edit),
    server/routes/settings.js (edit)
  </files>
  <read_first>
    server/index.js lines 159-255 (contains: auto-install-hooks block around 160-166 — KEEP that; import-history require on line 175 — REMOVE; periodic maintenance sweep uses findCompactionsInFile + importCompactions lines 204-222 — REMOVE the "Scan active sessions for new compaction entries" step; auto-import on startup around lines 241-255 — REMOVE entirely),
    server/routes/settings.js line 103 (internal require of importAllSessions — REMOVE the route/handler that uses it, or at minimum the require; confirm exact route)
  </read_first>
  <action>
    1. Edit server/index.js:
       - Remove the require line 175: `const { importCompactions, findCompactionsInFile } = require("../scripts/import-history");`
       - Inside the 2-minute periodic maintenance `setInterval` callback, REMOVE the "Scan active sessions for new compaction entries" block (currently the block starting with comment "// 2. Scan active sessions for new compaction entries" around line 198 through the close of its `for (const row of active)` loop around line 222). KEEP the memory-usage log and the stale-session cleanup block that precede it. Also remove the in-block `const active = cleanupDb.db.prepare("SELECT DISTINCT e.session_id ...").all();` — it exists only to feed the removed compaction scanner.
       - Remove the "Auto-import legacy sessions and backfill compaction tracking on startup" block starting around line 241: delete the `const { importAllSessions, backfillCompactions } = require("../scripts/import-history");` line and the entire `importAllSessions(dbModule).then(...).then(() => backfillCompactions(dbModule))...` promise chain. If the chain has a trailing `.catch()`, remove that too.
       - KEEP: the `installHooks` auto-install block (lines 160-166), the stale-session cleanup portion of the periodic sweep, the watchdog, `stopReplyPoller`, and all other unrelated code.
       - Ensure the remaining periodic sweep still compiles and runs — after removal, the setInterval body should contain only (a) memory-usage log, (b) stale-session cleanup loop.
    2. Edit server/routes/settings.js:
       - Inspect the handler around line 103 that invokes `importAllSessions`. If the whole route handler exists solely to trigger import-history (e.g. POST /api/settings/reimport-history or similar), REMOVE the handler + its router registration + any test that exercises it. If the require is buried inside a larger handler that does other work, remove just the import-history invocation and any response fields that depend on it; document the response-shape delta in the plan summary.
       - Leave every other route in this file untouched (this plan must NOT delete /api/settings/cleanup or rewrite /api/settings/export — those are owned by Plan 50.5-04).
    3. Run `node -e "require('./server/db'); require('./server/index'); setTimeout(()=>process.exit(0), 500);"` in a dry check equivalent: not required as acceptance, but confirm manually that `npm run test:server` passes.
  </action>
  <acceptance_criteria>
    - `grep -n "import-history\|importAllSessions\|importCompactions\|findCompactionsInFile\|backfillCompactions" server/index.js server/routes/settings.js` returns no matches
    - `grep -n "setInterval" server/index.js` shows the maintenance sweep still present (confirms we didn't accidentally delete too much)
    - `grep -n "installHooks\|stopReplyPoller\|findStaleSessions" server/index.js` each return matches (preserved code intact)
    - `npm run test:server` passes
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/api/gsd/projects` (or the server's test harness equivalent) returns 200 with unchanged response shape — verify via the existing projects route test in the test suite
    - `npm run mcp:typecheck &amp;&amp; npm run mcp:build` still pass (server deletions must not ripple into MCP)
  </acceptance_criteria>
  <verify>
    <automated>cd /data/home/gsddashboard &amp;&amp; npm run test:server &amp;&amp; npm run mcp:typecheck &amp;&amp; npm run mcp:build</automated>
  </verify>
  <done>Server boots and tests green with zero references to the deleted scripts; /api/gsd/projects response shape unchanged; MCP still builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server boot → filesystem | Removed require() of deleted scripts — any surviving reference would crash boot, so grep-before-delete is the primary control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.5.02-01 | Denial of Service | Server boot | mitigate | Task 2 removes all require() calls BEFORE Task 1's deletions are deployed; test:server gate blocks merge if anything is missed |
| T-50.5.02-02 | Tampering | Periodic maintenance sweep | mitigate | Preserve the stale-session cleanup branch; only remove the compaction-scanning branch; verify via grep that findStaleSessions is still called |
| T-50.5.02-03 | Information Disclosure | /api/settings/* surface | accept | This plan does not change /cleanup or /export (Plan 50.5-04 owns those); any removed endpoint here is a zero-caller maintenance route |
</threat_model>

<verification>
- npm run test:server passes
- npm run mcp:typecheck + npm run mcp:build pass
- /api/gsd/projects response unchanged shape
- Dashboard UI loads with no console errors post-deploy
</verification>

<success_criteria>
- CLN-04 acceptance: seed.js, import-history.js, and statusline/ deleted; server/index.js startup hook removed; no grep matches for deleted symbols in surviving source; server boots; MCP builds
</success_criteria>

<output>
After completion, create `.planning/phases/50.5-original-repo-cleanup/50.5-02-SUMMARY.md` summarizing: files deleted (with grep-proof of zero external callers), server/index.js edits, any route handler changes in settings.js, test results.
</output>
