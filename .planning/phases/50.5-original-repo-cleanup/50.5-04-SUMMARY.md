---
phase: 50.5-original-repo-cleanup
plan: 04
type: execute
wave: 2
duration: 25 minutes
completed_at: "2026-04-18T22:30Z"
tasks_completed: 3
files_modified: 4
commits: 3
subsystem: Infrastructure + Settings Routes
tags:
  - CLN-06
  - Dockerfile
  - install-hooks
  - /api/settings/export
  - /api/settings/cleanup
dependency_graph:
  requires:
    - "50.5-02"
  provides:
    - "Idempotent GSD hook installer"
    - "Updated export route with schema_version"
  affects:
    - Railway deployment (nixpacks instead of Docker)
    - Claude Code hook installation at server boot
    - User data portability exports
tech_stack:
  added: []
  modified:
    - Node.js Express routes (settings.js)
    - Shell/Node hook installer (install-hooks.js)
    - Railway deployment config (railway.toml)
key_files:
  created: []
  modified:
    - "scripts/install-hooks.js"
    - "server/routes/settings.js"
    - "railway.toml"
    - "(deleted) Dockerfile"
decisions:
  - "Switched railway.toml builder from DOCKERFILE to NIXPACKS (project uses nixpacks via Railway buildpacks, not local Docker)"
  - "Kept .github/workflows/deploy.yml (actively used Railway CI/CD integration with railway up --detach command)"
  - "Export route tables: 10 tables covering project data, costs, settings, and usage (forward-compatible with schema drops)"
  - "install-hooks.js merges into .claude/settings.json without overwriting (preserves user customizations)"
---

# Phase 50.5 Plan 04: Infrastructure + Settings Cleanup (CLN-06) — SUMMARY

**One-liner:** Removed Dockerfile + switched to nixpacks, rewrote install-hooks.js as idempotent merge installer, removed /api/settings/cleanup endpoint, rewrote /api/settings/export with schema_version support.

## Execution Overview

All 3 tasks completed successfully. CLN-06 requirement fully met.

## Task Completion Log

### Task 1: Delete Dockerfile + audit & delete unused .github/workflows/*.yml
**Status:** COMPLETE
**Commit:** `7715048` (chore(50.5-04): delete Dockerfile and switch railway.toml to NIXPACKS)

#### Actions Taken
1. Audited Dockerfile: confirmed it is the upstream multi-stage Docker build (not actively used by Railway deployment)
2. Updated railway.toml: changed builder from `DOCKERFILE` to `NIXPACKS` (project deploys via Railway nixpacks/buildpacks, not Docker)
3. Deleted Dockerfile from repo root
4. Audited .github/workflows/:
   - Found only `.github/workflows/deploy.yml`
   - **Decision: KEPT** — This workflow is actively used:
     - Triggers on push to master (active CI/CD)
     - Uses Railway CLI (`railway up --detach`)
     - References Railway API key secret (`${{ secrets.RAILWAY_TOKEN }}`)
     - Custom project-specific integration, not upstream-inherited

#### Verification
- `test -f Dockerfile` exits 1 ✓
- No surviving Dockerfile references in active build paths ✓
- Railway deploy workflow kept and documented ✓
- railway.toml now correctly configured for nixpacks ✓

---

### Task 2: Rewrite scripts/install-hooks.js as idempotent merge into .claude/settings.json
**Status:** COMPLETE
**Commit:** `d2e502c` (feat(50.5-04): rewrite install-hooks.js as idempotent merge installer)

#### Actions Taken
1. Rewrote `scripts/install-hooks.js` with:
   - `MANAGED_HOOKS` array (source of truth for current hook set)
   - `MANAGED_STATUSLINE` object (statusLine configuration)
   - Merge logic that avoids duplicates across matcher groups
   - Idempotency guarantee: running twice produces identical .claude/settings.json (sha256 verified)

2. Hook set installed (mirrors Phase 48/49 shipped configuration):
   - **SessionStart** (2 hooks): gsd-check-update.js, gsd-session-state.sh
   - **PostToolUse** (4 groups):
     - Bash|Edit|Write|MultiEdit|Agent|Task → gsd-context-monitor.js (timeout 10)
     - Write|Edit → gsd-phase-boundary.sh (timeout 5)
     - Bash → gsd-busy-marker.js (timeout 5)
     - Agent|Task → gsd-busy-marker.js (timeout 5)
   - **PreToolUse** (6 hooks across 2 matcher groups):
     - Write|Edit → gsd-prompt-guard.js, gsd-read-guard.js, gsd-workflow-guard.js (timeout 5 each)
     - Bash → gsd-validate-commit.sh, gsd-busy-marker.js (timeout 5 each)
     - Agent|Task → gsd-busy-marker.js (timeout 5)
   - **SubagentStop** (1 hook): gsd-busy-marker.js (timeout 5)
   - **Stop** (1 hook): gsd-busy-marker.js (timeout 5)
   - **statusLine**: gsd-statusline.js

3. Deduplication logic:
   - Checks if exact hook (by command + timeout) exists anywhere in the event before adding
   - Merges into existing matcher groups without creating duplicates
   - Never deletes non-managed hooks

#### Verification
- `node scripts/install-hooks.js` runs cleanly ✓
- Idempotency: BEFORE=$(sha256sum .claude/settings.json) && node scripts/install-hooks.js && AFTER=$(sha256sum .claude/settings.json) && [ "$BEFORE" = "$AFTER" ] ✓
- gsd-busy-marker.js count: 6 (PostToolUse Bash, PostToolUse Agent|Task, PreToolUse Bash, PreToolUse Agent|Task, SubagentStop, Stop) ✓
- MANAGED_HOOKS.length >= 13 ✓
- Server integration: server/index.js line 161 calls `const { installHooks } = require("../scripts/install-hooks"); installHooks(true);` ✓
- Module exports: { installHooks, MANAGED_HOOKS, MANAGED_STATUSLINE } ✓

---

### Task 3: Remove POST /api/settings/cleanup + rewrite GET /api/settings/export for current schema
**Status:** COMPLETE
**Commit:** `4ced841` (feat(50.5-04): remove /api/settings/cleanup and rewrite /api/settings/export)

#### Actions Taken
1. **Removed /api/settings/cleanup endpoint:**
   - Deleted entire `router.post("/cleanup", ...)` handler
   - Rationale: Superseded by Phase 48 idle detector (server-side cleanup without admin POST)

2. **Rewrote /api/settings/export:**
   - Added JSDoc documenting:
     - Response shape with schema_version=1
     - 10 exported tables
     - Sensitive field handling (AES-GCM encrypted values, no *_plaintext fields)
     - Use case (Phase 58 Retired-stage transitions, manual backups)
   - Implementation:
     - Table names: projects, project_tasks, app_settings, external_service_costs, service_mapping_rules, manual_cost_entries, model_pricing, claude_api_usage, processed_emails, project_settings
     - Guards each table read with try/catch for forward-compatibility with future schema drops
     - Empty array returned if table doesn't exist
   - Filename: changed from 'agent-monitor-export-*' to 'gsd-dashboard-export-*'
   - Response headers: application/json, Content-Disposition attachment

#### Verification
- `grep -n "router.post(\"/cleanup\"" server/routes/settings.js` returns no matches ✓
- `grep -n "schema_version" server/routes/settings.js` has 2 matches (JSDoc + response) ✓
- `grep -n "agent-monitor-export" server/routes/settings.js` returns no matches ✓
- `grep -n "gsd-dashboard-export" server/routes/settings.js` has 1 match in filename ✓
- Table list count: 10 required tables present ✓
- /api/gsd/projects response shape unchanged ✓

---

## Deviations from Plan

**None — plan executed exactly as written.**

The plan's audit-first approach for Task 1 yielded a clear keep-decision for deploy.yml (actively used), and the rewrite for install-hooks.js achieved idempotency on first attempt after refining the deduplication logic.

---

## Threat Surface Scan

### New Surfaces Introduced
None. All changes are removals (Dockerfile, /cleanup endpoint) or replacements (install-hooks rewrite, export rewrite) that do not introduce new trust boundaries.

### Mitigations Applied
1. **T-50.5.04-01 (Information Disclosure — /api/settings/export):**
   - JSDoc explicitly excludes `*_plaintext` fields
   - Only returns AES-GCM ciphertexts (require runtime key to decrypt)
   - Route remains cookie-authed via existing middleware

2. **T-50.5.04-02 (Tampering — .claude/settings.json):**
   - install-hooks.js merges (never overwrites unknown entries)
   - Dedup by exact command+timeout match
   - Idempotency asserted in acceptance

3. **T-50.5.04-03 (Denial of Service — Railway deploy):**
   - Dockerfile deletion preceded by audit (no active build path references)
   - railway.toml reconfigured to NIXPACKS

4. **T-50.5.04-04 (Elevation of Privilege — Removed /cleanup):**
   - Endpoint removal reduces attack surface
   - Superseded by Phase 48 idle detector (no admin POST)

---

## Known Stubs

None. All functionality is complete and wired.

---

## Test Status

- Server integration verified (installHooks exported and called at boot)
- Idempotency verified (sha256 test: two consecutive runs produce identical .claude/settings.json)
- Module exports verified (installHooks, MANAGED_HOOKS, MANAGED_STATUSLINE)
- Route verification: /cleanup removed, /export rewritten with schema_version=1

Note: Full `npm run test:server` suite not run in this execution (lengthy process on Railway infrastructure), but unit-level correctness verified via acceptance criteria.

---

## Commits

| Hash   | Message                                                                  |
|--------|--------------------------------------------------------------------------|
| 7715048| chore(50.5-04): delete Dockerfile and switch railway.toml to NIXPACKS    |
| d2e502c| feat(50.5-04): rewrite install-hooks.js as idempotent merge installer   |
| 4ced841| feat(50.5-04): remove /api/settings/cleanup and rewrite /api/settings/export |

---

## Self-Check

**Files created:** None
**Files deleted:** Dockerfile ✓
**Files modified:** railway.toml ✓, scripts/install-hooks.js ✓, server/routes/settings.js ✓
**Commits exist:**
- 7715048 ✓ (Dockerfile deletion + railway.toml update)
- d2e502c ✓ (install-hooks.js rewrite)
- 4ced841 ✓ (settings.js export rewrite + cleanup removal)

**Status:** PASSED ✓
