---
phase: 50.5-original-repo-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - "50.5-02"
files_modified:
  - .planning/phases/50.5-original-repo-cleanup/AUDIT-CLN-05.md
  - server/routes/events.js
  - server/routes/agents.js
  - server/index.js
  - server/db.js
autonomous: true
requirements:
  - CLN-05
must_haves:
  truths:
    - "AUDIT-CLN-05.md exists and documents: what was grepped, what was kept, what was dropped, and why, with concrete grep invocations and result counts"
    - "Every route confirmed dead by the audit (candidates: events.js, agents.js) is deleted from server/routes/ AND unmounted from server/index.js"
    - "Every SQL table confirmed dead by the audit (candidates: agents, events, stale token_usage rows if applicable) has a DROP TABLE IF EXISTS migration step in server/db.js bootstrap"
    - "Server boots against a fresh (empty) SQLite file — schema bootstrap succeeds without the dropped tables; subsequent boots idempotent"
    - "/api/gsd/projects still returns 200 with unchanged response shape"
    - "npm run test:server passes; npm run mcp:typecheck + npm run mcp:build pass"
  artifacts:
    - path: ".planning/phases/50.5-original-repo-cleanup/AUDIT-CLN-05.md"
      provides: "Audit log for CLN-05 — what was checked, kept, dropped, why"
      contains: "## Routes audited"
    - path: "server/db.js"
      provides: "Schema bootstrap with DROP TABLE IF EXISTS for dead tables"
      contains: "DROP TABLE IF EXISTS"
  key_links:
    - from: "server/index.js"
      to: "none"
      via: "unmounted dead routes"
      pattern: "require.*routes/(events|agents)"
---

<objective>
Audit the remaining upstream-inherited server surface (routes/events.js, routes/agents.js and the SQL tables they own: agents, events, stale token_usage rows) for live callers. For every confirmed-dead route and table: delete the route file, unmount it in server/index.js, and add a DROP TABLE IF EXISTS step to the schema bootstrap in server/db.js. Write the audit findings to AUDIT-CLN-05.md alongside the changes.

Purpose: CLN-05. This is the riskiest cleanup plan because it touches the DB schema. Audit-first gating is mandatory.
Output: AUDIT-CLN-05.md + smaller server surface + cleaner schema bootstrap, with zero behaviour change to preserved subsystems.
</objective>

<execution_context>
@/data/home/gsddashboard/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/gsddashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/50.5-original-repo-cleanup/50.5-CONTEXT.md
@CLAUDE.md
@.claude/rules/backend-node.md
@server/index.js
@server/db.js
@server/routes/events.js
@server/routes/agents.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit routes + tables, write AUDIT-CLN-05.md (audit-first gate)</name>
  <files>
    .planning/phases/50.5-original-repo-cleanup/AUDIT-CLN-05.md (create)
  </files>
  <read_first>
    server/routes/events.js (every exported endpoint),
    server/routes/agents.js (every exported endpoint),
    server/db.js (every table creation statement — identify every `CREATE TABLE` for agents, events, token_usage, and confirm which tables are listed in CONTEXT.md's "Preserve" allowlist),
    server/index.js (confirm mount points for events + agents routers on lines ~12-13 and ~56-57)
  </read_first>
  <action>
    1. For each candidate route file (server/routes/events.js, server/routes/agents.js):
       - Enumerate every HTTP verb + path the router exposes (e.g. GET /api/agents, POST /api/events/:id, etc.).
       - For each endpoint, run `grep -rn "/api/agents\|/api/events" client/ server/ mcp/ scripts/ .claude/ --include='*.js' --include='*.ts' --include='*.tsx' --include='*.json' --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.planning` and count active callers. Exclude: the route file itself, its own tests, and historical .planning/ references.
       - Additionally verify MCP doesn't call these endpoints: `grep -rn "agents\|events" mcp/src --include='*.ts'` — inspect each hit and classify as caller vs unrelated string.
       - Classify each endpoint: LIVE CALLER (must keep) / ZERO CALLER (safe to drop) / INTERNAL-ONLY (feeds the dropped table — drop).
       - A route file is DEAD if ALL its endpoints are ZERO CALLER / INTERNAL-ONLY.
    2. For each candidate table (`agents`, `events`, `token_usage`):
       - Run `grep -rn "INSERT INTO {table}\|SELECT .* FROM {table}\|UPDATE {table}\|DELETE FROM {table}\|FROM {table}\b" server/ scripts/ mcp/ --include='*.js' --include='*.ts' --exclude-dir=node_modules` for each table name.
       - Classify each write-site as "inside a DEAD route we're about to delete" vs "inside a preserved subsystem".
       - A table is DEAD if every surviving write/read is inside a route we're deleting OR inside server/index.js's periodic sweep (which Plan 50.5-02 already trimmed — re-grep server/index.js AFTER Plan 50.5-02 lands to confirm).
       - Explicit CONTEXT.md preserved-tables allowlist must be honoured: sessions, project_tasks, external_service_costs, service_mapping_rules, manual_cost_entries, app_settings, autopilot_runs, process_registry, model_pricing, claude_api_usage, classifier_feedback, classifier_overrides, processed_emails, project_settings, gsd_messages. None of these may appear in the DROP list.
       - For `token_usage`: CONTEXT.md mentions "stale token_usage rows" — this likely means DELETE stale rows, not DROP the table. Confirm whether the table is preserved (usage still tracked elsewhere via claude_api_usage + model_pricing) or dead. If preserved, the only action is a one-time cleanup DELETE of rows older than N days — do that only if the audit finds the table is still populated and never read. If in doubt, KEEP the table and note in AUDIT.
    3. Write `.planning/phases/50.5-original-repo-cleanup/AUDIT-CLN-05.md` with this exact structure:
       ```
       # AUDIT-CLN-05 — Dead Route + Schema Cleanup
       **Audited:** {ISO date}
       **Author:** Plan 50.5-03

       ## Routes audited
       ### server/routes/events.js
       - Endpoints enumerated: {list}
       - Grep command + result: {command} → {N matches}
       - Classification: DEAD / LIVE (per endpoint)
       - Decision: DELETE | KEEP (with rationale)

       ### server/routes/agents.js
       - (same structure)

       ## Tables audited
       ### agents
       - Write/read sites: {list with file:line}
       - Decision: DROP | KEEP (rationale)

       ### events
       - (same structure)

       ### token_usage
       - (same structure — expect KEEP unless audit shows otherwise)

       ## Preserved (CONTEXT.md allowlist — reaffirmed)
       {bullet list of tables not touched}

       ## Summary
       - Routes to delete: {list}
       - Tables to drop: {list}
       - Risk notes: {any ambiguous cases}
       ```
    4. Do NOT delete any route or drop any table in this task. This is the audit gate only.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/50.5-original-repo-cleanup/AUDIT-CLN-05.md` exits 0
    - `grep -c "^### " .planning/phases/50.5-original-repo-cleanup/AUDIT-CLN-05.md` ≥ 5 (at least routes events, agents + tables agents, events, token_usage audited)
    - `grep -n "Decision: DELETE\|Decision: KEEP\|Decision: DROP" .planning/phases/50.5-original-repo-cleanup/AUDIT-CLN-05.md` shows explicit decisions for every audited item
    - Preserved-allowlist section lists every table from CONTEXT.md's preserve set
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/50.5-original-repo-cleanup/AUDIT-CLN-05.md &amp;&amp; grep -q "## Summary" .planning/phases/50.5-original-repo-cleanup/AUDIT-CLN-05.md &amp;&amp; grep -q "Decision:" .planning/phases/50.5-original-repo-cleanup/AUDIT-CLN-05.md</automated>
  </verify>
  <done>Audit file committed with grep evidence and explicit DELETE/KEEP/DROP decisions per candidate.</done>
</task>

<task type="auto">
  <name>Task 2: Delete confirmed-dead routes + drop confirmed-dead tables per AUDIT-CLN-05.md</name>
  <files>
    server/routes/events.js (delete if DEAD per audit),
    server/routes/agents.js (delete if DEAD per audit),
    server/index.js (edit — unmount dead routes),
    server/db.js (edit — add DROP TABLE IF EXISTS for dead tables)
  </files>
  <read_first>
    .planning/phases/50.5-original-repo-cleanup/AUDIT-CLN-05.md (authoritative list of what to delete/drop),
    server/index.js lines 12-13 (require lines: agentsRouter, eventsRouter) and lines 56-57 (app.use mount points),
    server/db.js (identify the schema bootstrap function + where to add DROP TABLE IF EXISTS — typically inside the same function that runs CREATE TABLE IF NOT EXISTS),
    any test file under server/__tests__/ or server/routes/__tests__/ that exercises the routes being deleted
  </read_first>
  <action>
    1. Follow the audit's DELETE list exactly. For each route in the DELETE list:
       - Delete the route file: e.g. `rm server/routes/events.js` (only if audit says DELETE).
       - Edit server/index.js: remove the corresponding `const xRouter = require("./routes/x");` require line (around lines 12-13) AND the `app.use("/api/x", xRouter);` mount line (around lines 56-57).
       - Delete any test file that exercises that route (grep server/__tests__/ and server/routes/__tests__/ for the route filename or path).
    2. For each table in the DROP list from the audit:
       - Edit server/db.js schema bootstrap: BEFORE the `CREATE TABLE IF NOT EXISTS` block (or wherever the bootstrap function runs sequentially), add `db.exec("DROP TABLE IF EXISTS {tablename}");` for each table. Place these as a "One-time drops (CLN-05 cleanup, Phase 50.5)" section with a comment block above explaining provenance — per the backend-node.md transaction-boundary rule, group all drops into a single `db.transaction(() => { ... })()` call so the schema bootstrap remains atomic.
       - Ensure DROP TABLE IF EXISTS runs BEFORE any CREATE TABLE IF NOT EXISTS for preserved tables — drops are guarded with IF EXISTS so they are no-ops on fresh DBs and safe on stale DBs.
       - Also remove the corresponding `CREATE TABLE IF NOT EXISTS {tablename}` statement in server/db.js for each dropped table (otherwise the bootstrap would recreate it on next boot).
       - Remove any prepared-statement declarations in server/db.js that target the dropped tables (e.g. `stmts.listAgentsBySession`, `stmts.updateAgent`, `stmts.getAgent` if `agents` is dropped — grep `server/db.js` for these and remove). Also grep the wider server/ for each removed prepared-statement name to ensure no caller references it; if a caller exists and wasn't covered by the audit, STOP and update the audit.
    3. If the audit decided KEEP on any candidate, do NOT modify the corresponding file. Document the reasoning already lives in AUDIT-CLN-05.md.
    4. Run the smoke loop:
       - `rm -f /tmp/test-cln05.db && DATABASE_PATH=/tmp/test-cln05.db node -e "const db = require('./server/db'); console.log('bootstrap ok'); process.exit(0);"` → must print "bootstrap ok" (confirms fresh-DB bootstrap succeeds without the dropped tables). Use whatever env var the project uses for DB path — if the project hardcodes the path, skip this direct check and rely on the test suite.
       - `npm run test:server`
       - `npm run mcp:typecheck && npm run mcp:build`
  </action>
  <acceptance_criteria>
    - Every route in AUDIT's DELETE list: `test -f server/routes/{name}.js` exits 1
    - `grep -n "require.*routes/events\|require.*routes/agents" server/index.js` returns no matches (for each DELETED route; adjust per audit)
    - `grep -n "app.use.*\"/api/events\"\|app.use.*\"/api/agents\"" server/index.js` returns no matches (for each DELETED route)
    - `grep -n "DROP TABLE IF EXISTS" server/db.js` matches the set of tables on the DROP list (one entry per dropped table)
    - `grep -n "CREATE TABLE IF NOT EXISTS agents\b\|CREATE TABLE IF NOT EXISTS events\b" server/db.js` returns no matches (only if those tables are on the DROP list per audit)
    - Fresh-DB smoke: `rm -f /tmp/test-cln05.db &amp;&amp; DATABASE_PATH=/tmp/test-cln05.db node -e "require('./server/db'); console.log('ok');"` prints "ok" and exits 0
    - `npm run test:server` passes
    - `npm run mcp:typecheck &amp;&amp; npm run mcp:build` pass
    - /api/gsd/projects response shape unchanged — verified via the projects route test in the suite
    - Every preserved table (CONTEXT.md allowlist) still creates successfully on fresh bootstrap
  </acceptance_criteria>
  <verify>
    <automated>cd /data/home/gsddashboard &amp;&amp; rm -f /tmp/test-cln05.db &amp;&amp; DATABASE_PATH=/tmp/test-cln05.db node -e "require('./server/db'); console.log('ok');" &amp;&amp; npm run test:server &amp;&amp; npm run mcp:typecheck &amp;&amp; npm run mcp:build</automated>
  </verify>
  <done>Dead routes removed, dead tables dropped with IF EXISTS guards, fresh-DB bootstrap green, test suites green, /api/gsd/projects shape preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → SQLite schema | Schema DROP operations — destructive if applied to a preserved table |
| server → routes | Unmounting routes that still have callers would 404 production traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.5.03-01 | Tampering | SQLite schema | mitigate | Every DROP guarded with IF EXISTS; AUDIT-CLN-05.md documents grep-evidence for every DROP; Task 2 runs fresh-DB bootstrap smoke as acceptance; preserved-table allowlist enforced |
| T-50.5.03-02 | Denial of Service | Live dashboard traffic | mitigate | Task 1 audit gate; Task 2 only deletes routes on the DELETE list; MCP typecheck + build catches any cross-module import |
| T-50.5.03-03 | Repudiation | Audit log | mitigate | AUDIT-CLN-05.md committed alongside schema change — provides rollback/explanation provenance |
| T-50.5.03-04 | Information Disclosure | Dropped tables | accept | Local SQLite only, single-user dashboard, no PII in agents/events tables by current schema |
</threat_model>

<verification>
- npm run test:server passes
- npm run mcp:typecheck + npm run mcp:build pass
- Fresh-DB bootstrap smoke prints "ok"
- /api/gsd/projects returns 200 with unchanged shape after deploy
- Dashboard UI loads with no console errors
</verification>

<success_criteria>
- CLN-05 acceptance: AUDIT-CLN-05.md committed with grep evidence; dead routes removed; dead tables dropped with DROP TABLE IF EXISTS; schema bootstrap succeeds on fresh DB; preserved tables untouched; all test suites green
</success_criteria>

<output>
After completion, create `.planning/phases/50.5-original-repo-cleanup/50.5-03-SUMMARY.md` summarizing: audit outcomes (per-route, per-table decisions), deletions made, tables dropped, bootstrap smoke result, test results.
</output>
