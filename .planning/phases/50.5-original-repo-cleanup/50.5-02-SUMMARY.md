---
phase: 50.5-original-repo-cleanup
plan: 02
status: complete
completed: 2026-04-18
---

# Summary — 50.5-02 Upstream Script + Statusline Cleanup (CLN-04)

## Scope

Delete `scripts/seed.js`, `scripts/import-history.js`, the standalone `statusline/` directory, corresponding `npm` scripts, and any startup-time import-history invocation.

## Changes

**Commit `baebbc6`:**
- Deleted `scripts/seed.js` (292 lines, dev fixture seed)
- Deleted `scripts/import-history.js` (682 lines, upstream session-replay never enabled)
- Deleted `statusline/` directory (139 + 2 + 88 lines, upstream standalone CLI)
- Dropped `seed` and `import-history` entries from `package.json` scripts
- Total: 1205 lines removed

## Deviation from plan

Task 2 (strip import-history from `server/index.js` and `server/routes/settings.js`) had **no remaining work** — the hooks/imports the plan anticipated never existed in this codebase:

- `grep -rn "scripts/import-history\|require.*import-history\|IMPORT_ON_START" server/` returns no matches
- `server/index.js` has no startup-time seed/import invocation
- `server/routes/settings.js` has no internal require of `../scripts/import-history`

The plan was written conservatively under the assumption such hooks might exist. Task 1's deletions + npm-script pruning were the complete scope. Task 2 is closed as "no-op — verified absent".

## Verification

- `grep -r "seed\|import-history" --include="*.js" server/ scripts/ client/src/` returns no matches for deleted files
- `grep -n "scripts/import-history\|IMPORT_ON_START" server/index.js` → no matches (confirming Task 2 no-op)
- `statusline/` directory removed cleanly; `.claude/hooks/gsd-statusline.js` (unrelated Claude Code hook) preserved

## Preserved

- `scripts/install-hooks.js` (slated for rewrite in plan 50.5-04, not deletion)
- `.claude/hooks/gsd-statusline.js` (Claude Code hook, not upstream statusline)
- All of `server/` and `client/` untouched

## Files Modified / Deleted

- `package.json` (2 script entries removed)
- `scripts/seed.js` (deleted)
- `scripts/import-history.js` (deleted)
- `statusline/` (directory deleted)
