---
phase: 50.5-original-repo-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/App.tsx
  - client/src/components/Sidebar.tsx
  - client/src/pages/Settings.tsx
  - client/src/pages/KanbanBoard.tsx
  - client/src/pages/Sessions.tsx
  - client/src/pages/SessionDetail.tsx
  - client/src/pages/ActivityFeed.tsx
  - client/src/hooks/useNotifications.ts
  - client/src/components/board/KanbanBoard.tsx
  - client/src/components/board/__tests__/KanbanBoard.test.tsx
autonomous: true
requirements:
  - CLN-01
  - CLN-02
  - CLN-03
must_haves:
  truths:
    - "Navigating to /kanban, /sessions, /sessions/:id, /activity redirects to / (no 404s, no dead routes)"
    - "Sidebar does not render 'Agent Board', 'Sessions', or 'Activity Feed' links"
    - "Settings page has no browser-notification section; theme/idle-auto-close/Telegram sections remain intact"
    - "KanbanBoard is importable from client/src/components/board/KanbanBoard.tsx as a generic <KanbanBoard items columns onMove /> primitive with no agent-specific logic"
    - "No route references KanbanBoard (parked for Phase 61)"
    - "npm run test:client passes after changes"
  artifacts:
    - path: "client/src/components/board/KanbanBoard.tsx"
      provides: "Generic reusable board primitive"
      exports: ["KanbanBoard"]
    - path: "client/src/components/board/__tests__/KanbanBoard.test.tsx"
      provides: "Unit test for generic contract"
    - path: "client/src/App.tsx"
      provides: "Route table without dead routes + redirects for bookmarks"
  key_links:
    - from: "client/src/App.tsx"
      to: "none"
      via: "removed imports"
      pattern: "Sessions|SessionDetail|ActivityFeed|useNotifications"
---

<objective>
Delete legacy upstream views (Sessions, SessionDetail, ActivityFeed) and the /kanban route; relocate KanbanBoard.tsx to client/src/components/board/ as a generic reusable primitive stripped of agent-specific logic; remove the useNotifications hook and the browser-notifications section in Settings. Client-only scope — no server or infra changes.

Purpose: CLN-01, CLN-02, CLN-03. Clears dormant upstream fork UI before Phase 50 (Non-Programmer Mode Foundation) + Phase 54B (Unified Notification Centre) land.
Output: Slimmer client bundle, no dead routes, no browser-notification infra, KanbanBoard parked as a generic primitive for Phase 61.
</objective>

<execution_context>
@/data/home/gsddashboard/.claude/get-shit-done/workflows/execute-plan.md
@/data/home/gsddashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/50.5-original-repo-cleanup/50.5-CONTEXT.md
@CLAUDE.md
@.claude/rules/frontend-react.md
@.claude/rules/docs-markdown.md
@client/src/App.tsx
@client/src/components/Sidebar.tsx
@client/src/pages/Settings.tsx
@client/src/pages/KanbanBoard.tsx
@client/src/hooks/useNotifications.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Sessions/SessionDetail/ActivityFeed + drop dead routes + sidebar links + add redirects (CLN-01)</name>
  <files>
    client/src/pages/Sessions.tsx (delete),
    client/src/pages/SessionDetail.tsx (delete),
    client/src/pages/ActivityFeed.tsx (delete),
    client/src/App.tsx (edit),
    client/src/components/Sidebar.tsx (edit)
  </files>
  <read_first>
    client/src/App.tsx (current route table and imports),
    client/src/components/Sidebar.tsx (current nav entries),
    client/src/pages/Sessions.tsx (before deletion, confirm no unexpected re-exports),
    client/src/pages/__tests__/ (ensure no tests reference deleted pages)
  </read_first>
  <action>
    1. Delete the three page files: client/src/pages/Sessions.tsx, client/src/pages/SessionDetail.tsx, client/src/pages/ActivityFeed.tsx.
    2. Edit client/src/App.tsx:
       - Remove imports on lines 5-7: `import { Sessions } from "./pages/Sessions";`, `import { SessionDetail } from "./pages/SessionDetail";`, `import { ActivityFeed } from "./pages/ActivityFeed";`.
       - Remove the three `<Route>` entries currently on lines 68-70 for paths `sessions`, `sessions/:id`, `activity`.
       - Add redirect routes so existing bookmarks don't 404: `<Route path="sessions" element={<Navigate to="/" replace />} />`, `<Route path="sessions/:id" element={<Navigate to="/" replace />} />`, `<Route path="activity" element={<Navigate to="/" replace />} />`, `<Route path="kanban" element={<Navigate to="/" replace />} />` (kanban handled in Task 2 removal but the redirect belongs here to keep the edit coherent). Place redirects inside the existing `<Route element={<Layout ... />}>` block so they share the layout chrome on redirect tick.
    3. Edit client/src/components/Sidebar.tsx: remove the three nav entries on lines 35-37 (`/kanban` — "Agent Board", `/sessions` — "Sessions", `/activity` — "Activity Feed"). Leave all other entries (including icon imports already used elsewhere) intact. Remove any now-unused icon imports (`Columns3`, `FolderOpen`, `Activity` — check each: keep if used elsewhere in file).
    4. Update any sidebar test that asserts on the removed labels (client/src/components/__tests__/Sidebar.test.tsx — edit expectations to match new nav list; do not keep assertions for deleted entries).
  </action>
  <acceptance_criteria>
    - `test -f client/src/pages/Sessions.tsx` exits 1
    - `test -f client/src/pages/SessionDetail.tsx` exits 1
    - `test -f client/src/pages/ActivityFeed.tsx` exits 1
    - `grep -rn "from \"./pages/Sessions\"\|from \"./pages/SessionDetail\"\|from \"./pages/ActivityFeed\"" client/src` returns no matches
    - `grep -n "to=\"/sessions\"\|to=\"/activity\"\|to=\"/kanban\"" client/src/components/Sidebar.tsx` returns no matches
    - `grep -n "path=\"sessions\"\|path=\"sessions/:id\"\|path=\"activity\"" client/src/App.tsx` shows only Navigate redirect entries (not route element imports)
    - `npm run test:client` passes
  </acceptance_criteria>
  <verify>
    <automated>cd /data/home/gsddashboard &amp;&amp; npm run test:client</automated>
  </verify>
  <done>Dead client views removed, routes redirect to /, sidebar clean, client tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate KanbanBoard.tsx to client/src/components/board/ as generic primitive + remove /kanban route (CLN-02)</name>
  <files>
    client/src/components/board/KanbanBoard.tsx (create),
    client/src/components/board/__tests__/KanbanBoard.test.tsx (create),
    client/src/pages/KanbanBoard.tsx (delete),
    client/src/App.tsx (edit — remove import + Route)
  </files>
  <read_first>
    client/src/pages/KanbanBoard.tsx (identify agent-specific logic to strip),
    client/src/App.tsx (confirm /kanban Route + import to remove),
    client/src/components/__tests__/StatCard.test.tsx (reference for test file shape + vitest imports used in this codebase)
  </read_first>
  <action>
    1. Create client/src/components/board/KanbanBoard.tsx exporting a generic primitive with this contract:
       ```tsx
       export interface BoardColumn&lt;T&gt; { id: string; title: string; }
       export interface BoardItem { id: string; columnId: string; }
       export interface KanbanBoardProps&lt;T extends BoardItem&gt; {
         items: T[];
         columns: BoardColumn&lt;T&gt;[];
         onMove?: (itemId: string, toColumnId: string) =&gt; void;
         renderCard?: (item: T) =&gt; React.ReactNode;
       }
       export function KanbanBoard&lt;T extends BoardItem&gt;(props: KanbanBoardProps&lt;T&gt;): JSX.Element;
       ```
       Implementation: column layout (flex row, one column per columns[], Tailwind classes matching existing Dashboard style — bg-gray-900, rounded, p-3), filters items by columnId per column, calls `renderCard(item)` if provided else renders `<div className="p-2 bg-gray-800 rounded mb-2">{item.id}</div>` as default card, supports minimal HTML5 drag-drop (draggable cards + onDragOver/onDrop on columns) calling props.onMove(itemId, toColumnId). Strip ALL agent-specific logic from the old page (no `useSessions`, no `useAgents`, no tool-call rendering, no subagent expansion, no WebSocket subscription). This is a pure presentational primitive.
    2. Create client/src/components/board/__tests__/KanbanBoard.test.tsx with at minimum:
       - Renders one column div per columns[] entry (assert by column title text)
       - Renders each item inside its matching column (assert item.id appears inside the correct column's DOM subtree)
       - Calls `onMove(itemId, toColumnId)` once when a drop event is dispatched (use `fireEvent.dragStart` on the card + `fireEvent.drop` on the target column)
       - Custom `renderCard` is used when provided (assert custom text appears)
    3. Delete client/src/pages/KanbanBoard.tsx.
    4. Edit client/src/App.tsx: remove `import { KanbanBoard } from "./pages/KanbanBoard";` (line 4). The Navigate redirect for `path="kanban"` was added in Task 1 — confirm it is still present. Ensure no other file imports from `./pages/KanbanBoard`.
  </action>
  <acceptance_criteria>
    - `test -f client/src/components/board/KanbanBoard.tsx` exits 0
    - `test -f client/src/components/board/__tests__/KanbanBoard.test.tsx` exits 0
    - `test -f client/src/pages/KanbanBoard.tsx` exits 1
    - `grep -rn "from \"./pages/KanbanBoard\"\|from \"../pages/KanbanBoard\"" client/src` returns no matches
    - `grep -rn "useSessions\|useAgents\|subagent\|tool-call\|toolCall" client/src/components/board/KanbanBoard.tsx` returns no matches (confirms agent-specific logic stripped)
    - `grep -n "element={<KanbanBoard" client/src/App.tsx` returns no matches (no route references it)
    - `npm run test:client` passes (includes new KanbanBoard test)
  </acceptance_criteria>
  <verify>
    <automated>cd /data/home/gsddashboard &amp;&amp; npm run test:client</automated>
  </verify>
  <done>KanbanBoard lives under components/board as a generic primitive with a passing test, unreferenced by any route, parked for Phase 61.</done>
</task>

<task type="auto">
  <name>Task 3: Remove useNotifications hook + browser-notifications Settings section (CLN-03)</name>
  <files>
    client/src/hooks/useNotifications.ts (delete),
    client/src/App.tsx (edit — remove import + call),
    client/src/pages/Settings.tsx (edit — remove browser-notifications section)
  </files>
  <read_first>
    client/src/hooks/useNotifications.ts (identify its API + whether it returns anything callers use),
    client/src/App.tsx (currently calls useNotifications() on line 30),
    client/src/pages/Settings.tsx (lines 46-48 const NOTIF_KEY, lines 256-258 permission request, lines 747-874 rendered Notifications section — confirm the exact JSX block boundaries before editing)
  </read_first>
  <action>
    1. Delete client/src/hooks/useNotifications.ts.
    2. Edit client/src/App.tsx:
       - Remove import on line 17: `import { useNotifications } from "./hooks/useNotifications";`
       - Remove the call on line 30: `useNotifications();`
    3. Edit client/src/pages/Settings.tsx, removing:
       - The `NOTIF_KEY` const (around line 48) and any state/useEffect tied to it.
       - Any top-of-file `Notification`/`Permission` helper code around lines 256-258 (requestPermission effect).
       - The entire "Notifications" JSX section currently spanning approximately lines 745-875: the section heading "Notifications", the enable-browser-notifications toggle, permission status UI, "Send Test Notification" button, and the "Notifications are disabled — enable to get alerts for session events" footer. Preserve every other section (Pricing, Theme, Idle Auto-Close, Telegram, Data, Hooks). Update the page-description text on line 500 ("Manage pricing, notifications, data, and hooks") to remove "notifications, " — new text: "Manage pricing, data, and hooks".
       - Any `"Notification" in window` references left in Settings.tsx — remove them; no browser Notification API calls should remain in this file.
    4. Do NOT touch Telegram config, theme, idle-auto-close, or any other Settings section.
  </action>
  <acceptance_criteria>
    - `test -f client/src/hooks/useNotifications.ts` exits 1
    - `grep -rn "useNotifications" client/src` returns no matches
    - `grep -n "NOTIF_KEY\|Notification.requestPermission\|\"Notification\" in window\|Send Test Notification\|Enable Browser Notifications" client/src/pages/Settings.tsx` returns no matches
    - `grep -n "Manage pricing" client/src/pages/Settings.tsx` shows the updated string without "notifications, "
    - `npm run test:client` passes
    - `npm run build` (client build) succeeds without TypeScript errors referencing deleted symbols
  </acceptance_criteria>
  <verify>
    <automated>cd /data/home/gsddashboard &amp;&amp; npm run test:client &amp;&amp; npm run build</automated>
  </verify>
  <done>useNotifications hook gone, App no longer calls it, Settings has no browser-notification surface, build + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → client bundle | Removed dead routes — bookmarked URLs (/kanban, /sessions, /activity) now redirect via Navigate, preventing 404 confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50.5.01-01 | Denial of Service | Bookmarked /kanban or /sessions URLs | mitigate | Add Navigate redirect to / for each removed path (Task 1 action step 2) so users never hit a hard 404 |
| T-50.5.01-02 | Information Disclosure | Generic KanbanBoard primitive | accept | Pure presentational component; no data fetching, no auth context, no secret handling |
| T-50.5.01-03 | Tampering | Removed useNotifications | accept | Browser Notification API was opt-in client-only; removal eliminates attack surface rather than creating one |
</threat_model>

<verification>
- npm run test:client passes
- npm run build succeeds (no TS errors)
- Manual smoke after deploy: open /, confirm sidebar has no removed entries, navigate to /kanban and confirm redirect to /, open Settings and confirm no Notifications section, confirm other Settings sections render
- Dashboard UI loads with no console errors
</verification>

<success_criteria>
- CLN-01 acceptance: /kanban + sessions + activity routes removed, corresponding pages deleted, sidebar cleaned, redirects in place
- CLN-02 acceptance: KanbanBoard relocated to client/src/components/board/, generic contract, no route references, unit test covers generic contract
- CLN-03 acceptance: useNotifications deleted, Settings browser-notifications section removed, no Notification API references remain in client source
- All client tests pass; client build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/50.5-original-repo-cleanup/50.5-01-SUMMARY.md` summarizing: files deleted, files created, routes redirected, tests added, build verification.
</output>
