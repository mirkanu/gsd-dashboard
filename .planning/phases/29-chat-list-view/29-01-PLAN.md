---
phase: 29-chat-list-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/styles/chatscope-theme.css
  - client/src/main.tsx
  - client/src/lib/timeAgo.ts
  - client/src/lib/types.ts
  - server/routes/gsd.js
autonomous: true
requirements: [INF-04, CHAT-01, CHAT-02]

must_haves:
  truths:
    - "Chatscope components render with correct colors in both dark and light themes"
    - "The /api/gsd/projects endpoint returns lastMessage (content + timestamp) for each project"
    - "A timeAgo utility converts ISO timestamps into relative strings (just now, 5m, 3h, Yesterday)"
  artifacts:
    - path: "client/src/styles/chatscope-theme.css"
      provides: "Dark/light CSS variable overrides for all chatscope .cs-* selectors"
      min_lines: 20
    - path: "client/src/lib/timeAgo.ts"
      provides: "Relative timestamp utility function"
      exports: ["timeAgo"]
    - path: "server/routes/gsd.js"
      provides: "Extended projects endpoint with lastMessage per project"
    - path: "client/src/lib/types.ts"
      provides: "GsdProject type extended with display_name and lastMessage"
  key_links:
    - from: "client/src/styles/chatscope-theme.css"
      to: "client/src/main.tsx"
      via: "CSS import between chatscope styles and Tailwind"
      pattern: "import.*chatscope-theme"
    - from: "server/routes/gsd.js"
      to: "client/src/lib/types.ts"
      via: "lastMessage field on project response"
      pattern: "lastMessage"
---

<objective>
Create the foundation layer for the chat list view: chatscope dark/light theme overrides, server-side last-message data in the projects endpoint, and a timeAgo utility.

Purpose: Phase 29 replaces the kanban board with a chat conversation list. This plan provides the three prerequisites: themed chatscope components, per-project last-message data from the API, and relative timestamp formatting.

Output: chatscope-theme.css, extended /api/gsd/projects response, timeAgo.ts utility
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-chat-list-view/29-RESEARCH.md

@client/src/main.tsx
@client/src/lib/types.ts
@server/routes/gsd.js
@server/db.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chatscope theme CSS + timeAgo utility</name>
  <files>client/src/styles/chatscope-theme.css, client/src/main.tsx, client/src/lib/timeAgo.ts, client/src/lib/__tests__/timeAgo.test.ts</files>
  <behavior>
    - timeAgo(null) returns empty string
    - timeAgo(now - 30s) returns "just now"
    - timeAgo(now - 5min) returns "5m"
    - timeAgo(now - 3hours) returns "3h"
    - timeAgo(now - 30hours) returns "Yesterday"
    - timeAgo(now - 5days) returns short date like "Mar 29"
  </behavior>
  <action>
    1. Create `client/src/styles/chatscope-theme.css` with dark/light overrides for chatscope components. Override these selectors:
       - `.cs-conversation-list` — background to var(--surface-1), border to var(--border)
       - `.cs-conversation` — background to var(--surface-2), color inherit, border-bottom to var(--border)
       - `.cs-conversation:hover` — background to var(--surface-3)
       - `.cs-conversation--active` — background to var(--surface-3)
       - `.cs-conversation__name` — color inherit
       - `.cs-conversation__info` — color to muted (var(--surface-5) or text-gray-400 equivalent)
       - `.cs-conversation__last-activity-time` — color muted
       - `.cs-conversation__unread-dot` — background to var(--accent, #6366f1)
       - `.cs-conversation` border-left: none (clear default so wrapper border-l-4 is visible)
       - `.cs-conversation-list__scroll-wrapper` — touch-action: auto (fix mobile scroll per known issue)
       - Light theme variants scoped under `:root.light .cs-*` with appropriate light colors
       - Use CSS specificity (`:root .cs-conversation`) NOT !important

    2. Update `client/src/main.tsx` to import the theme CSS after chatscope styles but before index.css:
       ```
       import "@chatscope/chat-ui-kit-styles/dist/default/styles.min.css";
       import "./styles/chatscope-theme.css";  // ADD THIS LINE
       import "./index.css";
       ```

    3. Create `client/src/lib/timeAgo.ts` exporting a `timeAgo(dateStr: string | null): string` function:
       - null/empty -> ""
       - < 60s -> "just now"
       - < 3600s -> "{N}m"
       - < 86400s -> "{N}h"
       - < 172800s -> "Yesterday"
       - else -> short date (e.g., "Mar 29") using toLocaleDateString with month:'short', day:'numeric'

    4. Create `client/src/lib/__tests__/timeAgo.test.ts` with tests for all the behaviors above. Use vitest. Mock Date.now() for deterministic tests.
  </action>
  <verify>
    <automated>cd /data/home/gsddashboard/client && npx vitest run src/lib/__tests__/timeAgo.test.ts --reporter=verbose</automated>
  </verify>
  <done>chatscope-theme.css exists with dark+light overrides for all .cs-conversation* selectors; main.tsx imports it in correct order; timeAgo utility passes all 6 test cases</done>
</task>

<task type="auto">
  <name>Task 2: Extend projects endpoint with lastMessage data</name>
  <files>server/routes/gsd.js, client/src/lib/types.ts</files>
  <action>
    1. In `server/routes/gsd.js`, in the GET /projects handler, after the existing project data mapping, add a single SQL query to get the last visible message per project. Use a subquery approach on the existing `gsd_messages` table:
       ```sql
       SELECT project, content, message_type, created_at
       FROM gsd_messages m1
       WHERE m1.id = (
         SELECT MAX(m2.id) FROM gsd_messages m2
         WHERE m2.project = m1.project AND (m2.message_type IS NULL OR m2.message_type != 'hidden')
       )
       ```
       Execute this as a raw `db.prepare(...).all()` call. Build a Map<string, {content, message_type, created_at}> from the results. For each project in the response, add a `lastMessage` field:
       ```javascript
       lastMessage: lastMsgMap.get(name) || null
       ```
       The `lastMessage` object should have: `{ content: string, message_type: string, created_at: string }`. Truncate content to 100 chars for the preview.

    2. In `client/src/lib/types.ts`, update the `GsdProject` interface:
       - Add `display_name: string | null;` (already returned by server but missing from type)
       - Add `lastMessage: { content: string; message_type: string; created_at: string } | null;`

    3. Ensure the GSD_DATA_URL proxy path still works — the query runs on the local server, and Railway proxies the full response including the new field. No changes needed to proxy logic since it forwards the entire JSON response.
  </action>
  <verify>
    <automated>cd /data/home/gsddashboard && npm run build && npm run test:server</automated>
  </verify>
  <done>GET /api/gsd/projects returns lastMessage field per project (null if no messages); GsdProject type includes display_name and lastMessage; existing tests still pass; client builds without type errors</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds (client compiles with new types)
- `npm run test:server` passes (server tests including chatMessages)
- `cd client && npx vitest run src/lib/__tests__/timeAgo.test.ts` passes
- chatscope-theme.css imported in correct order in main.tsx
</verification>

<success_criteria>
- Chatscope CSS overrides exist for both dark and light themes targeting .cs-conversation* selectors
- main.tsx imports chatscope-theme.css between chatscope styles and index.css
- timeAgo utility works with all edge cases (null, seconds, minutes, hours, days)
- /api/gsd/projects response includes lastMessage per project
- GsdProject type includes display_name and lastMessage fields
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/29-chat-list-view/29-01-SUMMARY.md`
</output>
