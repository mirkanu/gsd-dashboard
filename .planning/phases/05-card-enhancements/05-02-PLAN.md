---
phase: 05-card-enhancements
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - client/src/pages/GSD.tsx
  - client/src/components/GsdDrawer.tsx
autonomous: true
requirements:
  - CARD-03

must_haves:
  truths:
    - "Clicking the card body (outside the URL link and expand button) opens a drawer scoped to that project"
    - "The drawer can be dismissed (closed)"
    - "The URL link click does not open the drawer"
    - "The expand button click does not open the drawer"
  artifacts:
    - path: "client/src/components/GsdDrawer.tsx"
      provides: "Drawer shell component accepting project prop"
      contains: "GsdDrawer"
    - path: "client/src/pages/GSD.tsx"
      provides: "selectedProject state and drawer wiring in GSD page"
      contains: "selectedProject"
  key_links:
    - from: "client/src/pages/GSD.tsx selectedProject state"
      to: "client/src/components/GsdDrawer.tsx"
      via: "project prop and onClose callback"
    - from: "client/src/pages/GSD.tsx ProjectCard onClick"
      to: "selectedProject setter"
      via: "onSelect callback prop"
---

<objective>
Wire card body clicks to open a project-scoped side drawer shell. The drawer component is a stub (Phase 6 fills the tab content).

Purpose: CARD-03 — clicking a card body opens a drawer so users can drill into project planning files. The stub drawer shows the project name and a close button; Phase 6 adds tabs with rendered markdown.
Output: GsdDrawer component + card click wiring + selectedProject state in GSD page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@client/src/pages/GSD.tsx
@client/src/lib/types.ts

<!-- Key interface from Plan 01 output -->
<!-- GsdProject now has: name, root, state, roadmap, requirements, version, liveUrl -->
</context>

<interfaces>
From client/src/lib/types.ts (after Plan 01):
```typescript
export interface GsdProject {
  name: string;
  root: string;
  state: GsdState | null;
  roadmap: { phases: GsdPhase[] } | null;
  requirements: GsdRequirements | null;
  version: string | null;
  liveUrl: string | null;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create GsdDrawer stub component</name>
  <files>client/src/components/GsdDrawer.tsx</files>
  <action>
    Create `client/src/components/GsdDrawer.tsx` as a drawer shell. Phase 6 will add the file tabs; this plan only provides the structural container.

    **Props interface:**
    ```typescript
    interface GsdDrawerProps {
      project: GsdProject;
      onClose: () => void;
    }
    ```

    **Behavior:**
    - Renders a fixed overlay on the right side of the screen (slide-in panel)
    - Overlay background: semi-transparent `fixed inset-0 bg-black/40 z-40` — clicking it calls `onClose`
    - Drawer panel: `fixed right-0 top-0 h-full w-full max-w-lg bg-surface-2 border-l border-border z-50 flex flex-col shadow-2xl`
    - Header: project name in a capitalized `text-base font-semibold text-gray-100` heading, plus a close button (`X` icon from lucide-react, `w-4 h-4`) — close button calls `onClose`. Header padding: `px-5 py-4 border-b border-border flex items-center justify-between flex-shrink-0`
    - Body: `flex-1 overflow-y-auto px-5 py-4` containing a `<p className="text-sm text-gray-500">File viewer coming in Phase 6.</p>` placeholder

    Import `GsdProject` type from `../lib/types`. Import `X` from lucide-react.

    Do NOT add keyboard escape listener in this plan — Phase 6 can add it when the drawer becomes functional.
  </action>
  <verify>
    <automated>npm run test:client 2>&1 | tail -10</automated>
  </verify>
  <done>GsdDrawer.tsx exists, renders a panel with project name header and close button, compiles without TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire card click to open drawer in GSD page</name>
  <files>client/src/pages/GSD.tsx</files>
  <action>
    In `client/src/pages/GSD.tsx`:

    1. **Add selectedProject state** to the `GSD` component:
       ```typescript
       const [selectedProject, setSelectedProject] = useState<GsdProject | null>(null);
       ```

    2. **Add onSelect prop to ProjectCard** — update the `ProjectCard` function signature to accept `onSelect: (project: GsdProject) => void` as a prop alongside `project`.

    3. **Make the card div clickable** — add `onClick={() => onSelect(project)}` and `className` addition `cursor-pointer` to the outermost card `div` in `ProjectCard`. The card `div` currently has `className="card flex flex-col gap-0 overflow-hidden"` — change to `"card flex flex-col gap-0 overflow-hidden cursor-pointer"`.

    4. **Protect existing interactive elements** — The existing expand/collapse `<button>` for the roadmap already handles its own click and will NOT need stopPropagation added (React bubbling stops at the button's own handler naturally — but to be safe, add `onClick={(e) => { e.stopPropagation(); setExpanded((v) => !v); }}` to that button, replacing the existing `onClick={() => setExpanded((v) => !v)}`). The live URL `<a>` tag already has `stopPropagation` from Plan 01.

    5. **Pass onSelect to each ProjectCard** in the `projects.map(...)` call:
       ```tsx
       <ProjectCard key={project.name} project={project} onSelect={setSelectedProject} />
       ```

    6. **Render GsdDrawer** — After the `</div>` that closes the project cards grid, add:
       ```tsx
       {selectedProject && (
         <GsdDrawer project={selectedProject} onClose={() => setSelectedProject(null)} />
       )}
       ```

    7. **Import GsdDrawer** at the top of `GSD.tsx`:
       ```typescript
       import { GsdDrawer } from "../components/GsdDrawer";
       ```

    Do NOT change the API call, loading state, error state, summary stats, or refresh button.
  </action>
  <verify>
    <automated>npm run test:client 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Clicking card body opens GsdDrawer for that project
    - URL link click (stopPropagation from Plan 01) does not open drawer
    - Roadmap expand button does not open drawer
    - Drawer close button sets selectedProject to null and hides drawer
    - npm run test:client passes
    - npm run build succeeds
  </done>
</task>

</tasks>

<verification>
Run `npm run test:client` — all tests pass.
Run `npm run build` — TypeScript compiles without errors.
Manual verification in dev mode:
1. Click a project card body → drawer slides in from right with project name
2. Click the X button → drawer closes
3. Click outside the drawer panel (overlay) → drawer closes
4. Click the live URL link on a card → opens new tab, no drawer
5. Click the roadmap expand toggle → expands roadmap, no drawer
</verification>

<success_criteria>
- GsdDrawer component exists and renders a stub panel with project name and close button
- Clicking card body opens drawer scoped to that project
- Existing interactive elements (URL link, expand button) do not open drawer
- Drawer closes via X button or overlay click
- All client tests pass; build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-card-enhancements/05-02-SUMMARY.md` using the summary template.
</output>
