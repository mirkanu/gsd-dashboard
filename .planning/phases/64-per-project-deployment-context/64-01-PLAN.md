# Plan 64-01: Update per-project deployment context after Hetzner migration

## Goal
Each project's Claude Code session currently has stale or missing deployment facts from before the
Hetzner migration (phase 62). This plan writes accurate infrastructure context into each project's
CLAUDE.md and GSD project memory so future Claude Code sessions start with correct knowledge.

## Context
**Hetzner VPS:** hostname `hetzner-vps`, IP `37.27.212.18`, Ubuntu 24.04 LTS
**Orchestration:** single `docker-compose.yml` at `/home/services/hetzner-vps/docker-compose.yml`
**Reverse proxy:** Cloudflare Tunnel (not nginx/caddy) — subdomains route via tunnel ingress rules
**Migration date:** ~2026-04-25 (phase 62)

## Deployment facts per project (source of truth for this plan)

| Project | What moved to Hetzner | What stayed elsewhere |
|---|---|---|
| debates | App (port 3000) + PostgreSQL (port 5433) | nothing — fully migrated |
| ynab | App (port 3001) + PostgreSQL | nothing — fully migrated |
| KidAI (admin) | kidai-admin (port 3002) + MongoDB + image-search-mcp (port 8080) | LibreChat — deliberately kept on Railway; admin manages it via Railway API |
| reforma | PostgreSQL only (pgvector, port 5432) | Frontend unchanged on Vercel |
| zoho-todoist-sync | Web (port 3003) + worker + Redis + PostgreSQL | nothing — fully migrated |
| gsddashboard | PM2 process (port 4820) + cloudflared tunnel | nothing — was already here |

## Tasks

### Task 1 — Update debates/CLAUDE.md hosting section
**File:** `/home/services/debates/CLAUDE.md`
**Action:** Replace `Hosting | Railway` row in the overview table and add a deployment section.

Change the table row:
```
| Hosting | Railway |
```
To:
```
| Hosting | Hetzner VPS (migrated from Railway, 2026-04-25) |
```

Add after the table (or in a new "## Deployment" section):
```markdown
## Deployment

| | |
|---|---|
| **Host** | Hetzner VPS — `hetzner-vps` (37.27.212.18) |
| **Container** | `debates` Docker container, port 3000 |
| **Database** | `debates-db` PostgreSQL container, port 5433 on host |
| **Compose file** | `/home/services/hetzner-vps/docker-compose.yml` |
| **Auto-deploy** | GitHub Actions → SSH → `git pull && docker compose up --build -d` |
| **Public URL** | Via Cloudflare Tunnel on gsdlabs.dev |
| **Railway** | No longer used — project can be deleted |
```

### Task 2 — Update zoho-todoist-sync/CLAUDE.md hosting section
**File:** `/home/services/zoho-todoist-sync/CLAUDE.md` (or `/data/home/zoho-todoist-sync/CLAUDE.md`)
**Action:** Find the "Runs as two Railway services" line at the top and update it, and add a deployment section.

Change:
```
Two-way sync between Zoho CRM Tasks (assigned to me) and a Todoist project. Runs as two Railway services (`web` + `worker`). No UI.
```
To:
```
Two-way sync between Zoho CRM Tasks (assigned to me) and a Todoist project. Runs as two Hetzner Docker services (`web` + `worker`). No UI.
```

Add a "## Deployment" section:
```markdown
## Deployment

Migrated from Railway to Hetzner VPS on 2026-04-25.

| | |
|---|---|
| **Host** | Hetzner VPS — `hetzner-vps` (37.27.212.18) |
| **Web container** | `zoho-sync-web`, port 3003 on host |
| **Worker container** | `zoho-sync-worker` (no exposed port) |
| **Redis** | `zoho-sync-redis` container |
| **Database** | `zoho-sync-db` PostgreSQL container |
| **Compose file** | `/home/services/hetzner-vps/docker-compose.yml` |
| **Railway** | No longer used |
```

### Task 3 — Create KidAI/CLAUDE.md
**File:** `/home/services/KidAI/CLAUDE.md`
**Action:** Create CLAUDE.md with project overview and accurate deployment facts.

KidAI is the parents admin panel for a LibreChat-based kids AI chat service.

```markdown
# KidAI — Codebase Orientation

KidAI is the **parents admin panel** for a kids AI chat service built on LibreChat. The admin
(this repo) handles parent accounts, child quotas, billing controls, and email alerts.
The LibreChat chat frontend is a **separate service**, still running on Railway.

## Stack

| Layer | Technology |
|---|---|
| Framework | Next.js (App Router, TypeScript) |
| Database | MongoDB 7 |
| Auth | NextAuth.js |
| Email | Resend |
| Image search | image-search-mcp sidecar (custom MCP server) |

## Deployment

**This repo (kidai-admin)** migrated to Hetzner VPS on 2026-04-25.
**LibreChat** deliberately stays on Railway — the admin manages it via Railway API.

| Service | Host | Detail |
|---|---|---|
| `kidai-admin` | Hetzner VPS | Port 3002, Docker container |
| `kidai-mongo` | Hetzner VPS | MongoDB 7 Docker container |
| `image-search-mcp` | Hetzner VPS | Port 8080, Docker sidecar |
| **LibreChat** | **Railway** | Managed via Railway API — env vars `RAILWAY_PROJECT_ID`, `RAILWAY_ENVIRONMENT_ID`, `RAILWAY_SERVICE_LIBRECHAT_ID` are intentional and required |

**Compose file:** `/home/services/hetzner-vps/docker-compose.yml`

## Key Integration: Railway API for LibreChat
The admin uses `RAILWAY_API_TOKEN` + `RAILWAY_PROJECT_ID` + `RAILWAY_SERVICE_LIBRECHAT_ID` to
start/stop LibreChat on demand (quota enforcement, cost control). These Railway env vars are
correct and necessary — LibreChat is not planned for Hetzner migration.

## Auto-deploy
GitHub Actions → SSH → `docker compose up --build -d`
```

### Task 4 — Create ynab/CLAUDE.md
**File:** `/home/services/ynab/CLAUDE.md`
**Action:** Create CLAUDE.md with project overview and deployment facts.

```markdown
# YNAB Automation — Codebase Orientation

YNAB automation service: syncs bank transactions, handles email-based expense capture,
and provides budget reporting. Next.js API + PostgreSQL.

## Stack

| Layer | Technology |
|---|---|
| Framework | Next.js (App Router, TypeScript) |
| Database | PostgreSQL 16 |
| Email inbound | Resend inbound webhook |
| Budget API | YNAB Personal Access Token |
| Auth | Auth.js (iron-session) |

## Deployment

Migrated from Railway to Hetzner VPS on 2026-04-25. Fully self-hosted.

| | |
|---|---|
| **Host** | Hetzner VPS — `hetzner-vps` (37.27.212.18) |
| **App container** | `ynab-api`, port 3001 on host |
| **Database** | `ynab-db` PostgreSQL container |
| **Compose file** | `/home/services/hetzner-vps/docker-compose.yml` |
| **Auto-deploy** | GitHub Actions → SSH → `docker compose up --build -d` |
| **Railway** | No longer used |

Note: `RAILWAY_API_TOKEN` env var in compose is a legacy remnant — verify whether it is still
referenced in code. If not, remove it.
```

### Task 5 — Create reforma/CLAUDE.md
**File:** `/home/services/reforma/CLAUDE.md`
**Action:** Create CLAUDE.md with project overview and hybrid deployment facts.

```markdown
# Reforma — Codebase Orientation

Reforma is a [describe project — update this line with actual purpose once known].

## Deployment

Reforma uses a **split deployment**: frontend on Vercel, database on Hetzner VPS.

| Layer | Host | Detail |
|---|---|---|
| **Frontend** | Vercel | Unchanged — no migration needed |
| **PostgreSQL** | Hetzner VPS | `reforma-db` container (pgvector/pgvector:pg16), port 5432 on host |
| **App server** | Vercel (via frontend) or N/A | No separate backend container on Hetzner |

**Compose file (DB only):** `/home/services/hetzner-vps/docker-compose.yml`

The database was migrated from Railway PostgreSQL to Hetzner on 2026-04-25.
Vercel's `DATABASE_URL` environment variable was updated to point at Hetzner IP `37.27.212.18:5432`.
Railway PostgreSQL for this project is no longer used.
```

### Task 6 — Write GSD project memory for KidAI
**File:** `/root/.claude/projects/-home-services-KidAI/memory/project_deployment.md` (create dir if needed)

Create a `memory/` directory and `MEMORY.md` index alongside the memory file, following the
standard GSD memory format:

```markdown
---
name: KidAI deployment — Hetzner + Railway split
description: Where KidAI services run after phase 62 migration — admin on Hetzner, LibreChat stays on Railway by design
type: project
---

kidai-admin, MongoDB, and image-search-mcp migrated to Hetzner VPS on 2026-04-25. LibreChat
deliberately stays on Railway — the admin uses Railway API (RAILWAY_PROJECT_ID,
RAILWAY_SERVICE_LIBRECHAT_ID) to manage LibreChat lifecycle. These Railway env vars are correct
and must not be removed.

**Why:** LibreChat migration was not in scope for phase 62; keeping it on Railway avoids downtime
risk for active child users.

**How to apply:** When suggesting infrastructure changes for KidAI, do not assume LibreChat is on
Hetzner. Railway API calls in the codebase are intentional.
```

## Verification

After each task, confirm the file exists and contains the correct hosting information:
- `grep -i hetzner /home/services/debates/CLAUDE.md`
- `grep -i hetzner /home/services/zoho-todoist-sync/CLAUDE.md` (or `/data/home/...`)
- `grep -i hetzner /home/services/KidAI/CLAUDE.md`
- `grep -i hetzner /home/services/ynab/CLAUDE.md`
- `grep -i hetzner /home/services/reforma/CLAUDE.md`
- `grep -i railway /home/services/KidAI/CLAUDE.md` — must confirm Railway is documented as intentional for LibreChat

## Out of scope
- Changing any running infrastructure
- Updating gsddashboard/CLAUDE.md — already correct (this repo)
- Migrating LibreChat off Railway — not planned
