---
phase: 12-session-state-indicators
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - client/src/pages/GSD.tsx
autonomous: true
requirements:
  - STAT-07

must_haves:
  truths:
    - "The summary stats row shows four cards: Working (green count), Waiting (amber count), Paused (red count), Archived (gray count)"
    - "The old Projects / Active / Complete stats cards are gone"
    - "Counts are computed from all projects including archived ones"
    - "Each stat card color matches the corresponding session state color scheme"
  artifacts:
    - path: "client/src/pages/GSD.tsx"
      provides: "Updated summary stats row with session-state counts"
      contains: "workingCount"
  key_links:
    - from: "client/src/pages/GSD.tsx (stats row)"
      to: "GsdProject.sessionState"
      via: "projects.filter(p => p.sessionState === 'working').length"
      pattern: "sessionState.*working"
---

<objective>
Replace the summary stats row with Working / Waiting / Paused / Archived session-state counts.

Purpose: The old Projects/Active/Complete stats are no longer meaningful now that session state is tracked. Replace them with the four session state counts using matching colors.

Output: Updated stats row in `GSD.tsx` — four cards with counts and matching colors derived from `sessionState`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-session-state-indicators/12-02-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts from codebase after plan 12-02 completes. -->

From client/src/pages/GSD.tsx (current stats section to replace):
```tsx
const totalProjects = projects.length;
const activeCount = projects.filter((p) => { ... }).length;
const completeCount = projects.filter((p) => { ... }).length;

{/* Summary stats */}
{!loading && !error && projects.length > 0 && (
  <div className="grid grid-cols-3 gap-3">
    {[
      { label: "Projects", value: totalProjects },
      { label: "Active", value: activeCount },
      { label: "Complete", value: completeCount },
    ].map(({ label, value }) => (
      <div key={label} className="card py-3 px-4 text-center">
        <div className="text-2xl font-bold text-gray-100">{value}</div>
        <div className="text-xs text-gray-500 mt-0.5">{label}</div>
      </div>
    ))}
  </div>
)}
```

SessionState color mapping (from plan 12-02):
- working  → emerald-400 (green)
- waiting  → amber-400
- paused   → red-400
- archived → gray-500
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace summary stats row with session-state counts</name>
  <files>client/src/pages/GSD.tsx</files>
  <action>
In `client/src/pages/GSD.tsx`, replace the existing stats variables and stats section:

**Remove** these variables (they are no longer used):
```typescript
const totalProjects = projects.length;
const activeCount = projects.filter(...).length;
const completeCount = projects.filter(...).length;
```

**Add** these replacements (keep near the same location, above the return):
```typescript
const workingCount  = projects.filter(p => p.sessionState === "working").length;
const waitingCount  = projects.filter(p => p.sessionState === "waiting").length;
const pausedCount   = projects.filter(p => p.sessionState === "paused").length;
const archivedCount = projects.filter(p => p.sessionState === "archived").length;
```

**Replace** the entire summary stats JSX block (the `{!loading && !error && projects.length > 0 && ...}` block) with:
```tsx
{!loading && !error && projects.length > 0 && (
  <div className="grid grid-cols-4 gap-3">
    <div className="card py-3 px-4 text-center">
      <div className="text-2xl font-bold text-emerald-400">{workingCount}</div>
      <div className="text-xs text-gray-500 mt-0.5">Working</div>
    </div>
    <div className="card py-3 px-4 text-center">
      <div className="text-2xl font-bold text-amber-400">{waitingCount}</div>
      <div className="text-xs text-gray-500 mt-0.5">Waiting</div>
    </div>
    <div className="card py-3 px-4 text-center">
      <div className="text-2xl font-bold text-red-400">{pausedCount}</div>
      <div className="text-xs text-gray-500 mt-0.5">Paused</div>
    </div>
    <div className="card py-3 px-4 text-center">
      <div className="text-2xl font-bold text-gray-500">{archivedCount}</div>
      <div className="text-xs text-gray-500 mt-0.5">Archived</div>
    </div>
  </div>
)}
```

Note: Change from `grid-cols-3` to `grid-cols-4` to accommodate the fourth stat. No other changes to the file.
  </action>
  <verify>
    <automated>npm run test:client 2>&1 | tail -20</automated>
  </verify>
  <done>Stats row shows 4 colored cards (Working/Waiting/Paused/Archived); old Projects/Active/Complete stats removed; client tests pass</done>
</task>

</tasks>

<verification>
After task completion:
- `npm run test:client` passes
- `npm run build` compiles without error
- Browser: stats row shows exactly 4 cards — Working (green), Waiting (amber), Paused (red), Archived (gray)
- Browser: counts reflect actual sessionState distribution across all projects
- No reference to `totalProjects`, `activeCount`, `completeCount` remains in the file
</verification>

<success_criteria>
- Stats row replaced with Working / Waiting / Paused / Archived counts
- Each count uses the correct color (emerald/amber/red/gray)
- 4-column grid replaces 3-column grid
- Old stats logic removed cleanly
- All client tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-session-state-indicators/12-03-SUMMARY.md`
</output>
