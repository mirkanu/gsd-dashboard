---
phase: 01-foundation-and-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/
  - server/
  - README.md
autonomous: true
requirements: [SETUP-01, SETUP-02]

must_haves:
  truths:
    - "Source files from Claude Code Agent Monitor exist in /data/home/gsddashboard"
    - "npm install completes without errors"
    - "Backend server starts (node server/index.js or equivalent)"
    - "Frontend builds without TypeScript/Webpack errors"
    - "Existing agent monitoring routes respond (sessions, cost, kanban)"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "express"
    - path: "src/"
      provides: "React frontend source"
    - path: "server/"
      provides: "Express backend source"
  key_links:
    - from: "package.json"
      to: "node_modules/"
      via: "npm install"
      pattern: "\"start\""
    - from: "server/"
      to: "http://localhost:PORT"
      via: "node server/index.js"
      pattern: "app\\.listen"
---

<objective>
Clone the upstream Claude Code Agent Monitor repository into /data/home/gsddashboard, install dependencies, and verify the existing agent monitoring features build and run correctly.

Purpose: Establishes the project base so all subsequent phases can build on working, tested code rather than scaffolding from scratch.
Output: Working fork with all upstream features intact, npm install passing, dev server startable.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone upstream repo and install dependencies</name>
  <files>package.json, package-lock.json, src/, server/, public/, README.md, .gitignore</files>
  <action>
    The repo at /data/home/gsddashboard is currently empty (only .git and .planning exist). Pull the upstream source directly into this directory without creating a subdirectory.

    Steps:
    1. Add the upstream remote: `git remote add upstream https://github.com/hoangsonww/Claude-Code-Agent-Monitor.git`
    2. Fetch upstream: `git fetch upstream`
    3. Merge upstream main into local master (ours strategy for any .planning conflicts): `git merge upstream/main --allow-unrelated-histories -m "feat(01-01): import upstream Claude Code Agent Monitor" --strategy-option=ours`
    4. If upstream uses a branch other than `main`, try `upstream/master`.
    5. Run `npm install` from /data/home/gsddashboard.
    6. If there is no package.json after merge (unlikely but possible), clone to a temp dir, copy files manually, then remove temp dir.

    Do NOT delete or overwrite the .planning/ directory — it must be preserved.
    Do NOT commit the merge yourself; the task verification will confirm state.
  </action>
  <verify>
    <automated>cd /data/home/gsddashboard && ls package.json src/ server/ 2>&amp;&amp; node -e "require('./package.json')" &amp;&amp; echo "Files OK"</automated>
  </verify>
  <done>package.json exists, src/ and server/ directories exist, npm install exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Verify existing features build and backend starts</name>
  <files>server/index.js (read-only verification)</files>
  <action>
    Verify the upstream app works as-is before any modifications:

    1. Run `npm run build` (or `npm run build --if-present`) to verify the React frontend compiles without errors. If build script is not present, run `npx tsc --noEmit` if TypeScript is used, or skip if pure JS.
    2. Identify the backend start command from package.json scripts (likely `npm start` or `node server/index.js`).
    3. Start the backend in the background: `node server/index.js &amp;` (or equivalent), wait 3 seconds, then curl `http://localhost:PORT/api/sessions` or the health endpoint.
    4. Confirm 200 response (or valid JSON response — 404 data is fine, just must not be a server crash).
    5. Kill the background server after verification.

    Note the actual port from server/index.js and use it in the curl command. Common values: 3001, 5000, 8080.
    If the server requires a running Claude session to return data, a 200 with empty array is acceptable.
  </action>
  <verify>
    <automated>cd /data/home/gsddashboard &amp;&amp; npm run build 2&gt;&amp;1 | tail -5 &amp;&amp; echo "BUILD_OK"</automated>
  </verify>
  <done>npm run build exits 0 and prints BUILD_OK; backend curl returns HTTP 200 or valid JSON (not a crash/connection refused)</done>
</task>

</tasks>

<verification>
After both tasks:
- `ls /data/home/gsddashboard/src /data/home/gsddashboard/server /data/home/gsddashboard/package.json` — all exist
- `npm run build` completes without error
- `node server/index.js` starts and responds on its configured port
- `.planning/` directory still intact (not overwritten by merge)
</verification>

<success_criteria>
Upstream repo is merged into /data/home/gsddashboard, dependencies installed, frontend builds, and backend starts. The .planning/ directory is preserved. All original agent monitoring features (sessions, cost, kanban) are present in the source code.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-configuration/01-01-SUMMARY.md`
</output>
