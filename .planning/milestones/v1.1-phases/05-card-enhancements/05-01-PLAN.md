---
phase: 05-card-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/types.ts
  - client/src/pages/GSD.tsx
autonomous: true
requirements:
  - CARD-01
  - CARD-02

must_haves:
  truths:
    - "Every project card shows a version badge (e.g. 'v1') sourced from the API response"
    - "Every project card shows a clickable live URL that opens in a new tab"
    - "The live URL link does not trigger any card-level click handler"
  artifacts:
    - path: "client/src/lib/types.ts"
      provides: "GsdProject interface with version and liveUrl fields"
      contains: "version: string | null"
    - path: "client/src/pages/GSD.tsx"
      provides: "Version badge and live URL rendered in ProjectCard header"
      contains: "version"
  key_links:
    - from: "client/src/lib/types.ts GsdProject.version"
      to: "client/src/pages/GSD.tsx ProjectCard"
      via: "project.version prop"
    - from: "client/src/lib/api.ts gsd.projects()"
      to: "client/src/pages/GSD.tsx state"
      via: "setProjects(data.projects)"
---

<objective>
Add version badge and live URL to each project card using the fields already returned by the Phase 4 API.

Purpose: CARD-01 and CARD-02 — surface version and live URL on each card so users can identify project state at a glance and jump to the live site.
Output: Updated GsdProject type and ProjectCard component with badge and clickable link.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@client/src/lib/types.ts
@client/src/pages/GSD.tsx
@client/src/lib/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend GsdProject type with version and liveUrl fields</name>
  <files>client/src/lib/types.ts, client/src/components/__tests__/GsdProject.test.ts</files>
  <behavior>
    - GsdProject interface has `version: string | null` field
    - GsdProject interface has `liveUrl: string | null` field
    - Existing fields (name, root, state, roadmap, requirements) are unchanged
  </behavior>
  <action>
    In `client/src/lib/types.ts`, add two fields to the `GsdProject` interface:

    ```typescript
    version: string | null;
    liveUrl: string | null;
    ```

    These fields are already returned by the API (added in Phase 4 plan 01). No API changes needed.

    The test file is a lightweight type-shape test — create `client/src/components/__tests__/GsdProject.test.ts` with a compile-time shape assertion using `satisfies` or a runtime check that confirms the fields exist on a mock object. Keep it minimal (3-4 assertions). Do NOT create a new test file if there is already a suitable place to add the assertions.
  </action>
  <verify>
    <automated>npm run test:client -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>GsdProject has version and liveUrl fields; all client tests pass; TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Render version badge and live URL in ProjectCard</name>
  <files>client/src/pages/GSD.tsx</files>
  <action>
    In `ProjectCard` inside `client/src/pages/GSD.tsx`:

    1. **Version badge** — In the card header row (the `flex items-start justify-between` div that contains the MapPin + project name), add a version badge immediately after the project name `h3`. Render it only when `project.version` is non-null. Use a `span` with classes that match the existing muted badge style: `text-[11px] font-medium px-1.5 py-0.5 rounded border bg-surface-3 text-gray-400 border-border`. Text content: `project.version` (e.g. "v1").

    2. **Live URL link** — Below the current-phase/milestone-name lines in the card header section, add a clickable link. Render only when `project.liveUrl` is non-null. Use an `<a>` tag with:
       - `href={project.liveUrl}`
       - `target="_blank"`
       - `rel="noopener noreferrer"`
       - `onClick={(e) => e.stopPropagation()}` — prevents bubbling to any future card-level click handler (CARD-03 depends on this)
       - Classes: `text-[11px] text-accent hover:text-accent/80 truncate pl-6 block mt-0.5 hover:underline`
       - Include the `ExternalLink` icon from lucide-react (w-3 h-3 inline mr-1 align-middle)

    Import `ExternalLink` from lucide-react at the top of the file (add to existing import).

    Do NOT add any drawer state or onClick to the card `div` itself — that is Plan 02's responsibility.
  </action>
  <verify>
    <automated>npm run test:client 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Version badge renders when project.version is set, hidden when null
    - Live URL renders as anchor opening in new tab when project.liveUrl is set, hidden when null
    - URL anchor has stopPropagation onClick
    - npm run test:client passes
    - npm run build succeeds (TypeScript clean)
  </done>
</task>

</tasks>

<verification>
Run `npm run test:client` — all tests pass.
Run `npm run build` — TypeScript compiles without errors.
Manually inspect: in dev mode (`npm run dev`), cards for projects that have version/liveUrl in PROJECT.md show badge and link; projects without them show nothing extra.
</verification>

<success_criteria>
- GsdProject type has version and liveUrl string | null fields
- ProjectCard renders version badge (e.g. "v1") when project.version is non-null
- ProjectCard renders a new-tab link for liveUrl when project.liveUrl is non-null
- URL link has stopPropagation to avoid interfering with future drawer wiring
- All client tests pass; build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-card-enhancements/05-01-SUMMARY.md` using the summary template.
</output>
