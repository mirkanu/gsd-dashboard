---
phase: 36-message-rendering-new-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/gsd/classifierPatterns.js
  - client/src/lib/types.ts
  - server/__tests__/classifier.test.js
autonomous: true
requirements: [CLS-04]

must_haves:
  truths:
    - "NEXT_UP type exists in classifier and TypeScript type system"
    - "Lines starting with triangle-right Next Up are classified as next_up"
    - "Lines containing /gsd: commands within next_up context are classified as next_up"
    - "Also available and /clear first lines are classified as next_up"
  artifacts:
    - path: "server/gsd/classifierPatterns.js"
      provides: "NEXT_UP patterns and MESSAGE_TYPES.NEXT_UP constant"
      contains: "NEXT_UP"
    - path: "client/src/lib/types.ts"
      provides: "next_up in MessageType union"
      contains: "next_up"
    - path: "server/__tests__/classifier.test.js"
      provides: "Tests for NEXT_UP classification"
      contains: "next_up"
  key_links:
    - from: "server/gsd/classifierPatterns.js"
      to: "client/src/lib/types.ts"
      via: "MESSAGE_TYPES.NEXT_UP maps to MessageType 'next_up'"
      pattern: "next_up"
---

<objective>
Add NEXT_UP as a recognized classifier type so "Next Up" blocks with GSD commands are classified distinctly from plain TEXT.

Purpose: Backend foundation for rendering NEXT_UP blocks as tappable command cards instead of plain text.
Output: Classifier patterns, TypeScript types, and tests for the new NEXT_UP message type.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/gsd/classifierPatterns.js
@client/src/lib/types.ts
@server/__tests__/classifier.test.js

<interfaces>
<!-- From server/gsd/classifierPatterns.js -->
```javascript
const MESSAGE_TYPES = {
  STAGE_BANNER: 'stage_banner',
  CHECKPOINT: 'checkpoint',
  COMPLETION: 'completion',
  ERROR: 'error',
  HIDDEN: 'hidden',
  TEXT: 'text',
};
// PATTERNS array: priority-ordered, first match wins
// classifyLine(rawLine) -> { msg_type, content, metadata } | null
```

<!-- From client/src/lib/types.ts -->
```typescript
export type MessageType = 'text' | 'stage_banner' | 'checkpoint' | 'completion' | 'error' | 'hidden';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NEXT_UP classifier patterns and TypeScript type</name>
  <files>server/gsd/classifierPatterns.js, client/src/lib/types.ts, client/src/components/ChatMessageRenderer.tsx</files>
  <action>
1. In `server/gsd/classifierPatterns.js`:
   - Add `NEXT_UP: 'next_up'` to MESSAGE_TYPES object
   - Add a new PATTERNS group for NEXT_UP, inserted BEFORE the Errors group (after Stage banners) so it has higher priority than TEXT fallthrough. The group should match:
     - `/^[►▶]\s*Next\s/i` — triangle-right Next Up header line
     - `/^\*\*Also available:\*\*/` — Also available block header
     - `/\/clear\s+first/` — Clear reminder line
     - `/^(?:Execute|Plan|Research|Verify|Quick):\s*`\/gsd:/` — GSD command suggestion lines like "Execute: `/gsd:execute-phase 36`"
     - `/`\/gsd:\S+`/` — Any backtick-wrapped /gsd: command (matches lines containing `/gsd:command-name`)
     - `/^[-•]\s*`\/gsd:/` — Bullet list items with /gsd commands

2. In `client/src/lib/types.ts`:
   - Add `'next_up'` to the MessageType union type

3. In `client/src/components/ChatMessageRenderer.tsx`:
   - Add `next_up: "Next Up"` to MESSAGE_TYPE_LABELS record
  </action>
  <verify>
    <automated>cd /data/home/gsddashboard && npm run test:server -- --grep "classif" 2>&1 | tail -20</automated>
  </verify>
  <done>MESSAGE_TYPES has NEXT_UP, PATTERNS array has next_up group matching Next Up headers/commands/also-available lines, TypeScript MessageType includes 'next_up', label map updated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add NEXT_UP classifier tests</name>
  <files>server/__tests__/classifier.test.js</files>
  <behavior>
    - "► Next Up" line classifies as next_up
    - "▶ Next Up" line classifies as next_up
    - Line containing backtick-wrapped `/gsd:execute-phase 36` classifies as next_up
    - "**Also available:**" line classifies as next_up
    - "/clear first" line classifies as next_up
    - "- `/gsd:quick fix bug`" bullet with command classifies as next_up
    - Plain text like "The next update will..." still classifies as text (not next_up)
    - "Execute: `/gsd:execute-phase 2`" classifies as next_up
  </behavior>
  <action>
Add a new `describe('NEXT_UP classification')` block to the existing classifier test file. Write tests for each behavior above using the existing `classifyLine` import. Follow the test patterns already in the file (import from classifierPatterns, call classifyLine, assert msg_type).
  </action>
  <verify>
    <automated>cd /data/home/gsddashboard && npm run test:server 2>&1 | tail -30</automated>
  </verify>
  <done>All NEXT_UP tests pass, no regressions in existing classifier tests</done>
</task>

</tasks>

<verification>
- `npm run test:server` passes with no regressions
- `classifyLine('► Next Up')` returns `{ msg_type: 'next_up', ... }`
- `classifyLine('Execute: `/gsd:execute-phase 2`')` returns `{ msg_type: 'next_up', ... }`
- Plain text lines still classified as 'text'
</verification>

<success_criteria>
- NEXT_UP exists as a classifier type with 6+ patterns covering Next Up headers, GSD commands, Also available, and clear reminders
- All new tests pass, zero regressions
- TypeScript MessageType union includes 'next_up'
</success_criteria>

<output>
After completion, create `.planning/phases/36-message-rendering-new-types/36-01-SUMMARY.md`
</output>
