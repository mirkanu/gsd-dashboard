# Continue Here — Phase 65 Wave 3

**Created:** 2026-05-04  
**Paused at:** Wave 2 complete, Wave 3 (Railway teardown) not started  
**Context used:** wave 2 complete

---

## Where we are

Phase 65 (`65-complete-railway-migration`) is executing. Waves 1 and 2 are **fully complete**.

### Wave 1 — DONE ✓

**Plan 01** (LibreChat + Meilisearch on Hetzner):
- `meilisearch` + `librechat` containers: Up, healthy
- Cloudflare DNS CNAME added: `librechat.gsdlabs.dev`
- Summary: `.planning/phases/65-complete-railway-migration/65-01-SUMMARY.md`

**Plan 02** (tunnel.sh cleanup + Railway data export):
- `tunnel.sh` already cleaned in remote commit `c414276`
- DALL-E images: 21 files downloaded → `/tmp/railway-dalle-images.tar.gz` (18 MB)
- project_tasks: Empty `[]` → `/tmp/railway-project-tasks.json` (Railway was proxy mode)
- Export log: `.planning/phases/65-complete-railway-migration/65-EXPORT-LOG.md`

---

### Wave 2 — DONE ✓

**Plan 03** (Image import + KidAI Railway decoupling):
- 21 DALL-E images imported into `hetzner-vps_librechat-images` Docker volume (strip-components=2)
- `server/routes/docker-ops.js` added to gsd-dashboard: POST `/api/docker/restart-librechat`
  - Protected by `LIBRECHAT_RESTART_SECRET` header
  - ufw rule added: allow 172.18.0.0/16 → port 4820/tcp
- KidAI routes updated: `redeploy-librechat/route.ts` + `prompt-editor/deploy/route.ts`
  - Railway GraphQL calls replaced with POST to `LIBRECHAT_RESTART_URL`
- docker-compose.yml kidai-admin: removed RAILWAY_* vars, added LIBRECHAT_RESTART_URL + extra_hosts
- `.env.production`: removed KIDAI_RAILWAY_* vars, added KIDAI_LIBRECHAT_RESTART_URL + secret
- Commits: `3ef04b4` (gsddashboard), `755eca6` (KidAI)
- Summary: `.planning/phases/65-complete-railway-migration/65-03-SUMMARY.md`

**Plan 04** (project_tasks verification):
- Confirmed empty — no import needed (Railway was in proxy mode)
- Summary: `.planning/phases/65-complete-railway-migration/65-04-SUMMARY.md`

---

## Next: Wave 3

### Plan 05 — Railway teardown (non-autonomous)

**Prerequisites (check before running):**
1. Verify Hetzner librechat is working end-to-end (login, chat, images)
2. Verify KidAI admin panel can restart librechat via the webhook
3. Confirm no Railway services are still needed

**Tasks:**
1. Delete Railway LibreChat service
2. Delete Railway GSD Dashboard service (if still running)
3. Remove any remaining Railway env vars from `.env.production`
4. Update KidAI `NEXT_PUBLIC_LIBRECHAT_URL` if still pointing to Railway
5. Cancel Railway subscription if no services remain

**IMPORTANT:** Do not run until Hetzner services are verified working by the user.

---

## Key config

- LibreChat: `https://librechat.gsdlabs.dev` (Hetzner, port 3004 internal)
- Restart webhook: `http://host.docker.internal:4820/api/docker/restart-librechat`
- Restart secret: in `.env.production` as `KIDAI_LIBRECHAT_RESTART_SECRET`
- docker-compose: always use `--env-file /home/services/.env.production`
- Image volume: `hetzner-vps_librechat-images` → `/app/client/public/images` in container

## Resume

```
/gsd-execute-phase 65 --wave 3
```
