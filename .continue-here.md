# Continue Here — GSD Dashboard

**Date paused:** 2026-03-23
**Branch:** master

## Where We Are

**Milestone:** v1.2 — GSD Stats & Live Data Pipeline (67% complete)
**Phase 8 complete.** Phase 9 is next (not started, not planned).

## Completed This Session

- **08-01**: Extended `server/gsd/readers.js` with `extractNextAction`, `readSummaryDates`, `computeVelocity`, `computeStreak`, `computeEstimatedCompletion`, `formatDaysEstimate`. `readState()` now returns `next_action`. `readProject()` now returns `velocity`, `streak`, `estimatedCompletion`. 4 new tests added and passing.
- **08-02**: Updated `GsdState` (added `next_action`) and `GsdProject` (added `velocity`, `streak`, `estimatedCompletion`) types. ProjectCard now shows: Blocked badge (red, header), next action line, stats row (velocity/streak/TTL). Blocked projects sort to top of grid. `npm run build` clean.
- **README.md rewritten** to accurately describe the project (dropped upstream copy, reflects GSD dashboard reality).

## Unresolved Note

- **Reforma `liveUrl` is null** on the GSD card. The `liveUrl` parser reads the first `https://` URL from the first 30 lines of `.planning/PROJECT.md`. Reforma's PROJECT.md has no URL there (only a Vercel deployment, URL not set). Fix: add the Vercel URL to the top section of `/data/home/reforma/.planning/PROJECT.md` (or to its `gsd-projects.json` entry if we add a config override field).

## Next Action

Run `/gsd:plan-phase 9` to plan Phase 9 (Active Session Pulse — show a live green pulse on a project card when a Claude Code session is active in that project's directory).

## Resume Command

```
/gsd:resume-work
```
